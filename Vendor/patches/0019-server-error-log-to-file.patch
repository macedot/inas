diff --git a/Vendor/libsmb2/lib/libsmb2.c b/Vendor/libsmb2/lib/libsmb2.c
index 5725a6f..2d26ef1 100644
--- a/Vendor/libsmb2/lib/libsmb2.c
+++ b/Vendor/libsmb2/lib/libsmb2.c
@@ -135,10 +135,8 @@ static const char SmbRpc[] = "SmbRpc";
 static const char SMBAppKey[] = "SMBAppKey";
 #endif
 
-const smb2_file_id compound_file_id = {
-        0xff, 0xff, 0xff, 0xff,  0xff, 0xff, 0xff, 0xff,
-        0xff, 0xff, 0xff, 0xff,  0xff, 0xff, 0xff, 0xff
-};
+const smb2_file_id compound_file_id = {0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff,
+                                       0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff};
 
 struct connect_data {
         smb2_command_cb cb;
@@ -167,8 +165,7 @@ struct smb2fh {
         int64_t end_of_file;
 };
 
-void
-smb2_close_context(struct smb2_context *smb2)
+void smb2_close_context(struct smb2_context *smb2)
 {
         if (smb2 == NULL) {
                 return;
@@ -195,13 +192,10 @@ smb2_close_context(struct smb2_context *smb2)
         smb2->session_up = 0;
 }
 
-static int
-send_session_setup_request(struct smb2_context *smb2,
-                           struct connect_data *c_data,
-                           unsigned char *buf, int len);
+static int send_session_setup_request(struct smb2_context *smb2, struct connect_data *c_data,
+                                      unsigned char *buf, int len);
 
-static void
-free_smb2dir(struct smb2_context *smb2, struct smb2dir *dir)
+static void free_smb2dir(struct smb2_context *smb2, struct smb2dir *dir)
 {
         while (dir->entries) {
                 struct smb2_dirent_internal *e = dir->entries->next;
@@ -216,11 +210,9 @@ free_smb2dir(struct smb2_context *smb2, struct smb2dir *dir)
         free(dir);
 }
 
-void
-smb2_seekdir(struct smb2_context *smb2, struct smb2dir *dir,
-                  long loc)
+void smb2_seekdir(struct smb2_context *smb2, struct smb2dir *dir, long loc)
 {
-        if (dir == NULL){
+        if (dir == NULL) {
                 return;
         }
         dir->current_entry = dir->entries;
@@ -232,8 +224,7 @@ smb2_seekdir(struct smb2_context *smb2, struct smb2dir *dir,
         }
 }
 
-long
-smb2_telldir(struct smb2_context *smb2, struct smb2dir *dir)
+long smb2_telldir(struct smb2_context *smb2, struct smb2dir *dir)
 {
         if (dir == NULL) {
                 return -EINVAL;
@@ -241,9 +232,7 @@ smb2_telldir(struct smb2_context *smb2, struct smb2dir *dir)
         return dir->index;
 }
 
-void
-smb2_rewinddir(struct smb2_context *smb2,
-                    struct smb2dir *dir)
+void smb2_rewinddir(struct smb2_context *smb2, struct smb2dir *dir)
 {
         if (dir == NULL) {
                 return;
@@ -252,9 +241,7 @@ smb2_rewinddir(struct smb2_context *smb2,
         dir->index = 0;
 }
 
-struct smb2dirent *
-smb2_readdir(struct smb2_context *smb2,
-             struct smb2dir *dir)
+struct smb2dirent *smb2_readdir(struct smb2_context *smb2, struct smb2dir *dir)
 {
         struct smb2dirent *ent;
         if ((dir == NULL) || (dir->current_entry == NULL)) {
@@ -268,8 +255,7 @@ smb2_readdir(struct smb2_context *smb2,
         return ent;
 }
 
-void
-smb2_closedir(struct smb2_context *smb2, struct smb2dir *dir)
+void smb2_closedir(struct smb2_context *smb2, struct smb2dir *dir)
 {
         if ((smb2 == NULL) || (dir == NULL)) {
                 return;
@@ -277,9 +263,7 @@ smb2_closedir(struct smb2_context *smb2, struct smb2dir *dir)
         free_smb2dir(smb2, dir);
 }
 
-static int
-decode_dirents(struct smb2_context *smb2, struct smb2dir *dir,
-               struct smb2_iovec *vec)
+static int decode_dirents(struct smb2_context *smb2, struct smb2dir *dir, struct smb2_iovec *vec)
 {
         struct smb2_dirent_internal *ent;
         struct smb2_fileidfulldirectoryinformation fs;
@@ -297,17 +281,15 @@ decode_dirents(struct smb2_context *smb2, struct smb2dir *dir,
                 ent = calloc(1, sizeof(struct smb2_dirent_internal));
                 if (ent == NULL) {
                         smb2_set_error(smb2, "Failed to allocate "
-                                       "dirent_internal");
+                                             "dirent_internal");
                         return -1;
                 }
                 SMB2_LIST_ADD(&dir->entries, ent);
 
-
                 tmp_vec.buf = &vec->buf[offset];
                 tmp_vec.len = vec->len - offset;
 
-                smb2_decode_fileidfulldirectoryinformation(smb2, &fs,
-                                                           &tmp_vec);
+                smb2_decode_fileidfulldirectoryinformation(smb2, &fs, &tmp_vec);
                 /* steal the name */
                 ent->dirent.name = fs.name;
                 ent->dirent.st.smb2_type = SMB2_TYPE_FILE;
@@ -335,15 +317,13 @@ decode_dirents(struct smb2_context *smb2, struct smb2dir *dir,
         return 0;
 }
 
-static void
-od_close_cb(struct smb2_context *smb2, int status,
-         void *command_data, void *private_data)
+static void od_close_cb(struct smb2_context *smb2, int status, void *command_data,
+                        void *private_data)
 {
         struct smb2dir *dir = private_data;
 
         if (status != SMB2_STATUS_SUCCESS) {
-                dir->cb(smb2, -nterror_to_errno(status),
-                        NULL, dir->cb_data);
+                dir->cb(smb2, -nterror_to_errno(status), NULL, dir->cb_data);
                 free_smb2dir(smb2, dir);
                 return;
         }
@@ -355,9 +335,7 @@ od_close_cb(struct smb2_context *smb2, int status,
         dir->cb(smb2, 0, dir, dir->cb_data);
 }
 
-static void
-query_cb(struct smb2_context *smb2, int status,
-         void *command_data, void *private_data)
+static void query_cb(struct smb2_context *smb2, int status, void *command_data, void *private_data)
 {
         struct smb2dir *dir = private_data;
         struct smb2_query_directory_reply *rep = command_data;
@@ -415,16 +393,14 @@ query_cb(struct smb2_context *smb2, int status,
                 return;
         }
 
-        smb2_set_nterror(smb2, status, "Query directory failed with (0x%08x) %s. %s",
-                       status, nterror_to_str(status),
-                       smb2_get_error(smb2));
+        smb2_set_nterror(smb2, status, "Query directory failed with (0x%08x) %s. %s", status,
+                         nterror_to_str(status), smb2_get_error(smb2));
         dir->cb(smb2, -nterror_to_errno(status), NULL, dir->cb_data);
         free_smb2dir(smb2, dir);
 }
 
-static void
-opendir_cb(struct smb2_context *smb2, int status,
-           void *command_data, void *private_data)
+static void opendir_cb(struct smb2_context *smb2, int status, void *command_data,
+                       void *private_data)
 {
         struct smb2dir *dir = private_data;
         struct smb2_create_reply *rep = command_data;
@@ -432,8 +408,8 @@ opendir_cb(struct smb2_context *smb2, int status,
         struct smb2_pdu *pdu;
 
         if (status != SMB2_STATUS_SUCCESS) {
-                smb2_set_nterror(smb2, status, "Opendir failed with (0x%08x) %s.",
-                               status, nterror_to_str(status));
+                smb2_set_nterror(smb2, status, "Opendir failed with (0x%08x) %s.", status,
+                                 nterror_to_str(status));
                 dir->cb(smb2, -nterror_to_errno(status), NULL, dir->cb_data);
                 free_smb2dir(smb2, dir);
                 return;
@@ -458,10 +434,9 @@ opendir_cb(struct smb2_context *smb2, int status,
         smb2_queue_pdu(smb2, pdu);
 }
 
-static struct smb2_pdu *
-_smb2_opendir_async(struct smb2_context *smb2, const char *path,
-                    smb2_command_cb cb, void *cb_data, void (*free_cb)(void *),
-                    int caller_frees_pdu)
+static struct smb2_pdu *_smb2_opendir_async(struct smb2_context *smb2, const char *path,
+                                            smb2_command_cb cb, void *cb_data,
+                                            void (*free_cb)(void *), int caller_frees_pdu)
 {
         struct smb2_create_request req;
         struct smb2dir *dir;
@@ -506,9 +481,8 @@ _smb2_opendir_async(struct smb2_context *smb2, const char *path,
         return pdu;
 }
 
-struct smb2_pdu *
-smb2_opendir_async_pdu(struct smb2_context *smb2, const char *path,
-                       smb2_command_cb cb, void *cb_data, void (*free_cb)(void *))
+struct smb2_pdu *smb2_opendir_async_pdu(struct smb2_context *smb2, const char *path,
+                                        smb2_command_cb cb, void *cb_data, void (*free_cb)(void *))
 {
         struct smb2_pdu *pdu;
 
@@ -516,9 +490,8 @@ smb2_opendir_async_pdu(struct smb2_context *smb2, const char *path,
         return pdu;
 }
 
-int
-smb2_opendir_async(struct smb2_context *smb2, const char *path,
-                   smb2_command_cb cb, void *cb_data)
+int smb2_opendir_async(struct smb2_context *smb2, const char *path, smb2_command_cb cb,
+                       void *cb_data)
 {
         struct smb2_pdu *pdu;
 
@@ -526,8 +499,7 @@ smb2_opendir_async(struct smb2_context *smb2, const char *path,
         return pdu ? 0 : -1;
 }
 
-extern void
-free_c_data(struct smb2_context *smb2, struct connect_data *c_data)
+extern void free_c_data(struct smb2_context *smb2, struct connect_data *c_data)
 {
         if (c_data->auth_data) {
                 if (smb2->sec == SMB2_SEC_NTLMSSP) {
@@ -541,7 +513,7 @@ free_c_data(struct smb2_context *smb2, struct connect_data *c_data)
         }
 
         if (smb2->connect_data == c_data) {
-            smb2->connect_data = NULL;  /* to prevent double-free in smb2_destroy_context */
+                smb2->connect_data = NULL; /* to prevent double-free in smb2_destroy_context */
         }
         free(c_data->utf8_unc);
         free(c_data->utf16_unc);
@@ -551,17 +523,15 @@ free_c_data(struct smb2_context *smb2, struct connect_data *c_data)
         free(c_data);
 }
 
-static void
-tree_connect_cb(struct smb2_context *smb2, int status,
-                void *command_data, void *private_data)
+static void tree_connect_cb(struct smb2_context *smb2, int status, void *command_data,
+                            void *private_data)
 {
         struct connect_data *c_data = private_data;
 
         if (status != SMB2_STATUS_SUCCESS) {
                 smb2_close_context(smb2);
-                smb2_set_nterror(smb2, status, "Tree Connect failed with (0x%08x) %s. %s",
-                               status, nterror_to_str(status),
-                               smb2_get_error(smb2));
+                smb2_set_nterror(smb2, status, "Tree Connect failed with (0x%08x) %s. %s", status,
+                                 nterror_to_str(status), smb2_get_error(smb2));
                 c_data->cb(smb2, -nterror_to_errno(status), NULL, c_data->cb_data);
                 free_c_data(smb2, c_data);
                 return;
@@ -571,15 +541,9 @@ tree_connect_cb(struct smb2_context *smb2, int status,
         free_c_data(smb2, c_data);
 }
 
-void smb2_derive_key(
-    uint8_t     *derivation_key,
-    uint32_t    derivation_key_len,
-    const char  *label,
-    uint32_t    label_len,
-    const char  *context,
-    uint32_t    context_len,
-    uint8_t     derived_key[SMB2_KEY_SIZE]
-    )
+void smb2_derive_key(uint8_t *derivation_key, uint32_t derivation_key_len, const char *label,
+                     uint32_t label_len, const char *context, uint32_t context_len,
+                     uint8_t derived_key[SMB2_KEY_SIZE])
 {
         unsigned char nul = 0;
         const uint32_t counter = htobe32(1);
@@ -588,8 +552,7 @@ void smb2_derive_key(
         HMACContext ctx;
         uint8_t digest[USHAMaxHashSize];
 
-        memcpy(input_key, derivation_key, MIN(sizeof(input_key),
-                                              derivation_key_len));
+        memcpy(input_key, derivation_key, MIN(sizeof(input_key), derivation_key_len));
         hmacReset(&ctx, SHA256, input_key, sizeof(input_key));
         hmacInput(&ctx, (unsigned char *)&counter, sizeof(counter));
         hmacInput(&ctx, (unsigned char *)label, label_len);
@@ -601,16 +564,13 @@ void smb2_derive_key(
 }
 
 /* MS-SMB2 3.2.5.2 */
-static void
-smb3_init_preauth_hash(struct smb2_context *smb2)
+static void smb3_init_preauth_hash(struct smb2_context *smb2)
 {
         memset(&smb2->preauthhash[0], 0, SMB2_PREAUTH_HASH_SIZE);
 }
 
 /* MS-SMB2 3.2.5.2 */
-int
-smb3_update_preauth_hash(struct smb2_context *smb2, int niov,
-                         struct smb2_iovec *iov)
+int smb3_update_preauth_hash(struct smb2_context *smb2, int niov, struct smb2_iovec *iov)
 {
         int i;
         USHAContext tctx;
@@ -659,62 +619,32 @@ static void smb2_create_signing_key(struct smb2_context *smb2)
         /* Derive the signing key from session key
          * This is based on negotiated protocol
          */
-        if (smb2->dialect == SMB2_VERSION_0202 ||
-            smb2->dialect == SMB2_VERSION_0210) {
+        if (smb2->dialect == SMB2_VERSION_0202 || smb2->dialect == SMB2_VERSION_0210) {
                 /* For SMB2 session key is the signing key */
-                memcpy(smb2->signing_key,
-                       smb2->session_key,
+                memcpy(smb2->signing_key, smb2->session_key,
                        MIN(smb2->session_key_size, SMB2_KEY_SIZE));
         } else if (smb2->dialect <= SMB2_VERSION_0302) {
-                smb2_derive_key(smb2->session_key,
-                                smb2->session_key_size,
-                                SMB2AESCMAC,
-                                sizeof(SMB2AESCMAC),
-                                SmbSign,
-                                sizeof(SmbSign),
-                                smb2->signing_key);
-                smb2_derive_key(smb2->session_key,
-                                smb2->session_key_size,
-                                SMB2AESCCM,
-                                sizeof(SMB2AESCCM),
-                                c2s_label,
-                                c2s_len,
-                                smb2->serverin_key);
-                smb2_derive_key(smb2->session_key,
-                                smb2->session_key_size,
-                                SMB2AESCCM,
-                                sizeof(SMB2AESCCM),
-                                s2c_label,
-                                s2c_len,
-                                smb2->serverout_key);
+                smb2_derive_key(smb2->session_key, smb2->session_key_size, SMB2AESCMAC,
+                                sizeof(SMB2AESCMAC), SmbSign, sizeof(SmbSign), smb2->signing_key);
+                smb2_derive_key(smb2->session_key, smb2->session_key_size, SMB2AESCCM,
+                                sizeof(SMB2AESCCM), c2s_label, c2s_len, smb2->serverin_key);
+                smb2_derive_key(smb2->session_key, smb2->session_key_size, SMB2AESCCM,
+                                sizeof(SMB2AESCCM), s2c_label, s2c_len, smb2->serverout_key);
         } else if (smb2->dialect > SMB2_VERSION_0302) {
-                smb2_derive_key(smb2->session_key,
-                                smb2->session_key_size,
-                                SMBSigningKey,
-                                sizeof(SMBSigningKey),
-                                (char *)smb2->preauthhash,
-                                SMB2_PREAUTH_HASH_SIZE,
-                                smb2->signing_key);
-                smb2_derive_key(smb2->session_key,
-                                smb2->session_key_size,
-                                c2s_label,
-                                c2s_len,
-                                (char *)smb2->preauthhash,
-                                SMB2_PREAUTH_HASH_SIZE,
+                smb2_derive_key(smb2->session_key, smb2->session_key_size, SMBSigningKey,
+                                sizeof(SMBSigningKey), (char *)smb2->preauthhash,
+                                SMB2_PREAUTH_HASH_SIZE, smb2->signing_key);
+                smb2_derive_key(smb2->session_key, smb2->session_key_size, c2s_label, c2s_len,
+                                (char *)smb2->preauthhash, SMB2_PREAUTH_HASH_SIZE,
                                 smb2->serverin_key);
-                smb2_derive_key(smb2->session_key,
-                                smb2->session_key_size,
-                                s2c_label,
-                                s2c_len,
-                                (char *)smb2->preauthhash,
-                                SMB2_PREAUTH_HASH_SIZE,
+                smb2_derive_key(smb2->session_key, smb2->session_key_size, s2c_label, s2c_len,
+                                (char *)smb2->preauthhash, SMB2_PREAUTH_HASH_SIZE,
                                 smb2->serverout_key);
         }
 }
 
-static void
-session_setup_cb(struct smb2_context *smb2, int status,
-                 void *command_data, void *private_data)
+static void session_setup_cb(struct smb2_context *smb2, int status, void *command_data,
+                             void *private_data)
 {
         struct connect_data *c_data = private_data;
         struct smb2_session_setup_reply *rep = command_data;
@@ -722,12 +652,10 @@ session_setup_cb(struct smb2_context *smb2, int status,
         struct smb2_pdu *pdu;
         int ret;
 
-        if (status == SMB2_STATUS_MORE_PROCESSING_REQUIRED &&
-            rep->security_buffer) {
+        if (status == SMB2_STATUS_MORE_PROCESSING_REQUIRED && rep->security_buffer) {
                 smb3_update_preauth_hash(smb2, smb2->in.niov - 1, &smb2->in.iov[1]);
-                if ((ret = send_session_setup_request(
-                                smb2, c_data, rep->security_buffer,
-                                rep->security_buffer_length)) < 0) {
+                if ((ret = send_session_setup_request(smb2, c_data, rep->security_buffer,
+                                                      rep->security_buffer_length)) < 0) {
                         smb2_close_context(smb2);
                         c_data->cb(smb2, ret, NULL, c_data->cb_data);
                         free_c_data(smb2, c_data);
@@ -736,10 +664,9 @@ session_setup_cb(struct smb2_context *smb2, int status,
                 return;
         } else if (status != SMB2_STATUS_SUCCESS) {
                 smb2_close_context(smb2);
-                smb2_set_nterror(smb2, status, "Session setup failed with (0x%08x) %s",
-                                status, nterror_to_str(status));
-                c_data->cb(smb2, -nterror_to_errno(status), NULL,
-                                c_data->cb_data);
+                smb2_set_nterror(smb2, status, "Session setup failed with (0x%08x) %s", status,
+                                 nterror_to_str(status));
+                c_data->cb(smb2, -nterror_to_errno(status), NULL, c_data->cb_data);
                 free_c_data(smb2, c_data);
                 return;
         }
@@ -751,7 +678,7 @@ session_setup_cb(struct smb2_context *smb2, int status,
         }
 
 #ifdef HAVE_LIBKRB5
-       if (smb2->sec == SMB2_SEC_KRB5) {
+        if (smb2->sec == SMB2_SEC_KRB5) {
                 /* For NTLM the status will be
                  * SMB2_STATUS_MORE_PROCESSING_REQUIRED and a second call to
                  * gss_init_sec_context will complete the gss session.
@@ -761,20 +688,17 @@ session_setup_cb(struct smb2_context *smb2, int status,
                  * At this point SMB2 layer reported success already, so we
                  * ignore krb5 errors.
                  */
-                krb5_session_request(smb2, c_data->auth_data,
-                                         rep->security_buffer,
-                                         rep->security_buffer_length);
-       }
+                krb5_session_request(smb2, c_data->auth_data, rep->security_buffer,
+                                     rep->security_buffer_length);
+        }
 #endif
 
-
         if (smb2->sign || smb2->seal || smb2->dialect == SMB2_VERSION_0311) {
                 uint8_t zero_key[SMB2_KEY_SIZE] = {0};
                 int have_valid_session_key = 1;
 
                 if (smb2->sec == SMB2_SEC_NTLMSSP) {
-                        if (ntlmssp_get_session_key(c_data->auth_data,
-                                                    &smb2->session_key,
+                        if (ntlmssp_get_session_key(c_data->auth_data, &smb2->session_key,
                                                     &smb2->session_key_size) < 0) {
                                 have_valid_session_key = 0;
                         }
@@ -787,12 +711,14 @@ session_setup_cb(struct smb2_context *smb2, int status,
                 }
 #endif
                 /* check if the session key is proper */
-                if (smb2->session_key == NULL || memcmp(smb2->session_key, zero_key, SMB2_KEY_SIZE) == 0) {
+                if (smb2->session_key == NULL ||
+                    memcmp(smb2->session_key, zero_key, SMB2_KEY_SIZE) == 0) {
                         have_valid_session_key = 0;
                 }
                 if (smb2->sign && have_valid_session_key == 0) {
                         smb2_close_context(smb2);
-                        smb2_set_error(smb2, "Signing required by server. Session "
+                        smb2_set_error(smb2,
+                                       "Signing required by server. Session "
                                        "Key is not available %s",
                                        smb2_get_error(smb2));
                         c_data->cb(smb2, -EACCES, NULL, c_data->cb_data);
@@ -813,7 +739,7 @@ session_setup_cb(struct smb2_context *smb2, int status,
                 if (smb2->sign && !(smb2->hdr.flags & SMB2_FLAGS_SIGNED)) {
                         smb2_close_context(smb2);
                         smb2_set_error(smb2, "Session setup reply is not "
-                                       "signed but signing is required");
+                                             "signed but signing is required");
                         c_data->cb(smb2, -EACCES, NULL, c_data->cb_data);
                         free_c_data(smb2, c_data);
                         return;
@@ -823,8 +749,7 @@ session_setup_cb(struct smb2_context *smb2, int status,
                         uint8_t signature[16] _U_;
 
                         memcpy(&signature[0], &smb2->in.iov[1].buf[48], 16);
-                        if (smb2_calc_signature(smb2, &smb2->in.iov[1].buf[48],
-                                                &smb2->in.iov[1],
+                        if (smb2_calc_signature(smb2, &smb2->in.iov[1].buf[48], &smb2->in.iov[1],
                                                 smb2->in.niov - 1) < 0) {
                                 c_data->cb(smb2, -EINVAL, NULL, c_data->cb_data);
                                 free_c_data(smb2, c_data);
@@ -833,7 +758,7 @@ session_setup_cb(struct smb2_context *smb2, int status,
                         if (memcmp(&signature[0], &smb2->in.iov[1].buf[48], 16)) {
                                 smb2_close_context(smb2);
                                 smb2_set_error(smb2, "Wrong signature in received "
-                                               "PDU");
+                                                     "PDU");
                                 c_data->cb(smb2, -EINVAL, NULL, c_data->cb_data);
                                 free_c_data(smb2, c_data);
                                 return;
@@ -842,9 +767,9 @@ session_setup_cb(struct smb2_context *smb2, int status,
         }
 
         memset(&req, 0, sizeof(struct smb2_tree_connect_request));
-        req.flags       = 0;
+        req.flags = 0;
         req.path_length = 2 * c_data->utf16_unc->len;
-        req.path        = c_data->utf16_unc->val;
+        req.path = c_data->utf16_unc->val;
 
         if (!smb2->passthrough) {
                 pdu = smb2_cmd_tree_connect_async(smb2, &req, tree_connect_cb, c_data);
@@ -855,8 +780,7 @@ session_setup_cb(struct smb2_context *smb2, int status,
                         return;
                 }
                 smb2_queue_pdu(smb2, pdu);
-        }
-        else {
+        } else {
                 /* if user wants raw data she probably doesnt want us to
                  * do an implicit tree-connect, so just end here
                  */
@@ -866,10 +790,8 @@ session_setup_cb(struct smb2_context *smb2, int status,
 }
 
 /* Returns 0 for success and -errno for failure */
-static int
-send_session_setup_request(struct smb2_context *smb2,
-                           struct connect_data *c_data,
-                           unsigned char *buf, int len)
+static int send_session_setup_request(struct smb2_context *smb2, struct connect_data *c_data,
+                                      unsigned char *buf, int len)
 {
         struct smb2_pdu *pdu;
         struct smb2_session_setup_request req;
@@ -879,30 +801,24 @@ send_session_setup_request(struct smb2_context *smb2,
         req.security_mode = (uint8_t)smb2->security_mode;
 
         if (smb2->sec == SMB2_SEC_NTLMSSP) {
-                if (ntlmssp_generate_blob(NULL, smb2, time(NULL), c_data->auth_data,
-                                          buf, len,
-                                          &req.security_buffer,
-                                          &req.security_buffer_length) < 0) {
+                if (ntlmssp_generate_blob(NULL, smb2, time(NULL), c_data->auth_data, buf, len,
+                                          &req.security_buffer, &req.security_buffer_length) < 0) {
                         smb2_close_context(smb2);
                         return -1;
                 }
         }
 #ifdef HAVE_LIBKRB5
         else {
-                if (krb5_session_request(smb2, c_data->auth_data,
-                                         buf, len) < 0) {
+                if (krb5_session_request(smb2, c_data->auth_data, buf, len) < 0) {
                         smb2_close_context(smb2);
                         return -1;
                 }
-                req.security_buffer_length =
-                        krb5_get_output_token_length(c_data->auth_data);
-                req.security_buffer =
-                        krb5_get_output_token_buffer(c_data->auth_data);
+                req.security_buffer_length = krb5_get_output_token_length(c_data->auth_data);
+                req.security_buffer = krb5_get_output_token_buffer(c_data->auth_data);
         }
 #endif
 
-        pdu = smb2_cmd_session_setup_async(smb2, &req,
-                                           session_setup_cb, c_data);
+        pdu = smb2_cmd_session_setup_async(smb2, &req, session_setup_cb, c_data);
         if (pdu == NULL) {
                 smb2_close_context(smb2);
                 return -ENOMEM;
@@ -912,9 +828,8 @@ send_session_setup_request(struct smb2_context *smb2,
         return 0;
 }
 
-static void
-negotiate_cb(struct smb2_context *smb2, int status,
-             void *command_data, void *private_data)
+static void negotiate_cb(struct smb2_context *smb2, int status, void *command_data,
+                         void *private_data)
 {
         struct connect_data *c_data = private_data;
         struct smb2_negotiate_reply *rep = command_data;
@@ -925,12 +840,10 @@ negotiate_cb(struct smb2_context *smb2, int status,
 
         if (status != SMB2_STATUS_SUCCESS) {
                 smb2_close_context(smb2);
-                smb2_set_nterror(smb2, status, "Negotiate failed with (0x%08x) %s. %s",
-                               status, nterror_to_str(status),
-                               smb2_get_error(smb2));
+                smb2_set_nterror(smb2, status, "Negotiate failed with (0x%08x) %s. %s", status,
+                                 nterror_to_str(status), smb2_get_error(smb2));
                 /* calls connect_cb */
-                c_data->cb(smb2, -nterror_to_errno(status), NULL,
-                           c_data->cb_data);
+                c_data->cb(smb2, -nterror_to_errno(status), NULL, c_data->cb_data);
                 free_c_data(smb2, c_data);
                 return;
         }
@@ -943,10 +856,10 @@ negotiate_cb(struct smb2_context *smb2, int status,
         }
 
         smb2->max_transact_size = rep->max_transact_size;
-        smb2->max_read_size     = rep->max_read_size;
-        smb2->max_write_size    = rep->max_write_size;
-        smb2->dialect           = rep->dialect_revision;
-        smb2->cypher            = rep->cypher;
+        smb2->max_read_size = rep->max_read_size;
+        smb2->max_write_size = rep->max_write_size;
+        smb2->dialect = rep->dialect_revision;
+        smb2->cypher = rep->cypher;
 
         /*
          * We used to hard-fail here if smb2->seal was requested and
@@ -961,10 +874,9 @@ negotiate_cb(struct smb2_context *smb2, int status,
          * on this bit alone.
          */
 
-        if (smb2->sign &&
-            !(rep->security_mode & SMB2_NEGOTIATE_SIGNING_ENABLED)) {
+        if (smb2->sign && !(rep->security_mode & SMB2_NEGOTIATE_SIGNING_ENABLED)) {
                 smb2_set_error(smb2, "Signing requested but server "
-                               "does not support signing.");
+                                     "does not support signing.");
                 smb2_close_context(smb2);
                 c_data->cb(smb2, -ENOMEM, NULL, c_data->cb_data);
                 free_c_data(smb2, c_data);
@@ -991,18 +903,16 @@ negotiate_cb(struct smb2_context *smb2, int status,
          * mechanisms are supported if caller hasn't explicitly set security
          */
         spnego_mechs = 0;
-        if (smb2->sec == SMB2_SEC_UNDEFINED &&
-                        rep->security_buffer && rep->security_buffer_length) {
-                smb2_spnego_unwrap_gssapi(smb2, rep->security_buffer,
-                        rep->security_buffer_length, 1,
-                        NULL, &spnego_mechs);
+        if (smb2->sec == SMB2_SEC_UNDEFINED && rep->security_buffer &&
+            rep->security_buffer_length) {
+                smb2_spnego_unwrap_gssapi(smb2, rep->security_buffer, rep->security_buffer_length,
+                                          1, NULL, &spnego_mechs);
 #ifdef HAVE_LIBKRB5
                 if (spnego_mechs & SPNEGO_MECHANISM_KRB5) {
                         smb2->sec = SMB2_SEC_KRB5;
                 }
 #endif
-                if (smb2->sec == SMB2_SEC_UNDEFINED &&
-                                (spnego_mechs & SPNEGO_MECHANISM_NTLMSSP)) {
+                if (smb2->sec == SMB2_SEC_UNDEFINED && (spnego_mechs & SPNEGO_MECHANISM_NTLMSSP)) {
                         smb2->sec = SMB2_SEC_NTLMSSP;
                 }
         }
@@ -1015,35 +925,25 @@ negotiate_cb(struct smb2_context *smb2, int status,
         }
 
         if (smb2->sec == SMB2_SEC_NTLMSSP) {
-                c_data->auth_data = ntlmssp_init_context(smb2->user,
-                                                         smb2->password,
-                                                         smb2->domain,
-                                                         smb2->workstation,
-                                                         smb2->client_challenge);
+                c_data->auth_data = ntlmssp_init_context(smb2->user, smb2->password, smb2->domain,
+                                                         smb2->workstation, smb2->client_challenge);
         }
 #ifdef HAVE_LIBKRB5
         else {
-                c_data->auth_data = krb5_negotiate_reply(smb2,
-                                                         c_data->server,
-                                                         smb2->domain,
-                                                         c_data->user,
-                                                         smb2->password);
+                c_data->auth_data = krb5_negotiate_reply(smb2, c_data->server, smb2->domain,
+                                                         c_data->user, smb2->password);
                 /*
                  * If Kerberos cannot be initialized (no realm, no creds, …)
                  * but the server also offered NTLMSSP, fall back so guest and
                  * password auth still work. Without this the client closes
                  * after negotiate and never sends SessionSetup.
                  */
-                if (c_data->auth_data == NULL &&
-                    (spnego_mechs & SPNEGO_MECHANISM_NTLMSSP)) {
+                if (c_data->auth_data == NULL && (spnego_mechs & SPNEGO_MECHANISM_NTLMSSP)) {
                         smb2_set_error(smb2, "");
                         smb2->sec = SMB2_SEC_NTLMSSP;
-                        c_data->auth_data = ntlmssp_init_context(
-                                                         smb2->user,
-                                                         smb2->password,
-                                                         smb2->domain,
-                                                         smb2->workstation,
-                                                         smb2->client_challenge);
+                        c_data->auth_data =
+                            ntlmssp_init_context(smb2->user, smb2->password, smb2->domain,
+                                                 smb2->workstation, smb2->client_challenge);
                 }
         }
 #endif
@@ -1062,17 +962,15 @@ negotiate_cb(struct smb2_context *smb2, int status,
         }
 }
 
-static void
-connect_cb(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void connect_cb(struct smb2_context *smb2, int status, void *command_data _U_,
+                       void *private_data)
 {
         struct connect_data *c_data = private_data;
         struct smb2_negotiate_request req;
         struct smb2_pdu *pdu;
 
         if (status != 0) {
-                smb2_set_error(smb2, "Socket connect failed with %d",
-                               status);
+                smb2_set_error(smb2, "Socket connect failed with %d", status);
                 c_data->cb(smb2, -status, NULL, c_data->cb_data);
                 free_c_data(smb2, c_data);
                 return;
@@ -1090,10 +988,8 @@ connect_cb(struct smb2_context *smb2, int status,
          * auto-detect, but tolerate a server that doesn't support it).
          */
         if (smb2->seal_requested != SMB2_SEAL_NONE &&
-            (smb2->version == SMB2_VERSION_ANY  ||
-             smb2->version == SMB2_VERSION_ANY3 ||
-             smb2->version == SMB2_VERSION_0300 ||
-             smb2->version == SMB2_VERSION_0302 ||
+            (smb2->version == SMB2_VERSION_ANY || smb2->version == SMB2_VERSION_ANY3 ||
+             smb2->version == SMB2_VERSION_0300 || smb2->version == SMB2_VERSION_0302 ||
              smb2->version == SMB2_VERSION_0311)) {
                 req.capabilities |= SMB2_GLOBAL_CAP_ENCRYPTION;
         }
@@ -1140,11 +1036,8 @@ connect_cb(struct smb2_context *smb2, int status,
         smb2_queue_pdu(smb2, pdu);
 }
 
-int
-smb2_connect_share_async(struct smb2_context *smb2,
-                         const char *server,
-                         const char *share, const char *user,
-                         smb2_command_cb cb, void *cb_data)
+int smb2_connect_share_async(struct smb2_context *smb2, const char *server, const char *share,
+                             const char *user, smb2_command_cb cb, void *cb_data)
 {
         struct connect_data *c_data;
         int err;
@@ -1208,8 +1101,7 @@ smb2_connect_share_async(struct smb2_context *smb2,
                 smb2_set_error(smb2, "Failed to strdup(user)");
                 return -ENOMEM;
         }
-        if (asprintf(&c_data->utf8_unc, "\\\\%s\\%s", c_data->server,
-                     c_data->share) < 0) {
+        if (asprintf(&c_data->utf8_unc, "\\\\%s\\%s", c_data->server, c_data->share) < 0) {
                 free_c_data(smb2, c_data);
                 smb2_set_error(smb2, "Failed to allocate unc string.");
                 return -ENOMEM;
@@ -1217,8 +1109,7 @@ smb2_connect_share_async(struct smb2_context *smb2,
 
         c_data->utf16_unc = smb2_utf8_to_utf16(c_data->utf8_unc);
         if (c_data->utf16_unc == NULL) {
-                smb2_set_error(smb2, "Count not convert UNC:[%s] into UTF-16",
-                               c_data->utf8_unc);
+                smb2_set_error(smb2, "Count not convert UNC:[%s] into UTF-16", c_data->utf8_unc);
                 free_c_data(smb2, c_data);
                 return -ENOMEM;
         }
@@ -1235,22 +1126,19 @@ smb2_connect_share_async(struct smb2_context *smb2,
         return 0;
 }
 
-static void
-free_smb2fh(struct smb2_context *smb2, struct smb2fh *fh)
+static void free_smb2fh(struct smb2_context *smb2, struct smb2fh *fh)
 {
         free(fh);
 }
 
-static void
-open_cb(struct smb2_context *smb2, int status,
-        void *command_data, void *private_data)
+static void open_cb(struct smb2_context *smb2, int status, void *command_data, void *private_data)
 {
         struct smb2fh *fh = private_data;
         struct smb2_create_reply *rep = command_data;
 
         if (status != SMB2_STATUS_SUCCESS) {
-                smb2_set_nterror(smb2, status, "Open failed with (0x%08x) %s.",
-                               status, nterror_to_str(status));
+                smb2_set_nterror(smb2, status, "Open failed with (0x%08x) %s.", status,
+                                 nterror_to_str(status));
                 fh->cb(smb2, -nterror_to_errno(status), NULL, fh->cb_data);
                 free_smb2fh(smb2, fh);
                 return;
@@ -1263,9 +1151,9 @@ open_cb(struct smb2_context *smb2, int status,
 
 static struct smb2_pdu *
 _smb2_open_async_with_oplock_or_lease(struct smb2_context *smb2, const char *path, int flags,
-                uint8_t oplock_level, uint32_t lease_state, smb2_lease_key lease_key,
-                smb2_command_cb cb, void *cb_data, void (*free_cb)(void *),
-                int caller_frees_pdu)
+                                      uint8_t oplock_level, uint32_t lease_state,
+                                      smb2_lease_key lease_key, smb2_command_cb cb, void *cb_data,
+                                      void (*free_cb)(void *), int caller_frees_pdu)
 {
         struct smb2fh *fh;
         struct smb2_create_request req;
@@ -1293,7 +1181,7 @@ _smb2_open_async_with_oplock_or_lease(struct smb2_context *smb2, const char *pat
         if (flags & O_CREAT) {
                 if (flags & O_EXCL) {
                         create_disposition = SMB2_FILE_CREATE;
-                } else if(flags & O_TRUNC) {
+                } else if (flags & O_TRUNC) {
                         create_disposition = SMB2_FILE_OVERWRITE_IF;
                 } else {
                         create_disposition = SMB2_FILE_OPEN_IF;
@@ -1308,18 +1196,16 @@ _smb2_open_async_with_oplock_or_lease(struct smb2_context *smb2, const char *pat
 
         /* desired access */
         switch (flags & O_ACCMODE) {
-                case O_RDWR:
-                case O_WRONLY:
-                        desired_access |= SMB2_FILE_WRITE_DATA |
-                                SMB2_FILE_WRITE_EA |
-                                SMB2_FILE_WRITE_ATTRIBUTES;
-                        if ((flags & O_ACCMODE) == O_WRONLY)
-                                break;
-                case O_RDONLY:
-                        desired_access |= SMB2_FILE_READ_DATA |
-                                SMB2_FILE_READ_EA |
-                                SMB2_FILE_READ_ATTRIBUTES;
+        case O_RDWR:
+        case O_WRONLY:
+                desired_access |=
+                    SMB2_FILE_WRITE_DATA | SMB2_FILE_WRITE_EA | SMB2_FILE_WRITE_ATTRIBUTES;
+                if ((flags & O_ACCMODE) == O_WRONLY)
                         break;
+        case O_RDONLY:
+                desired_access |=
+                    SMB2_FILE_READ_DATA | SMB2_FILE_READ_EA | SMB2_FILE_READ_ATTRIBUTES;
+                break;
         }
 
 #ifdef O_DIRECTORY
@@ -1354,11 +1240,11 @@ _smb2_open_async_with_oplock_or_lease(struct smb2_context *smb2, const char *pat
                 req.create_context = calloc(1, SMB2_CREATE_REQUEST_LEASE_SIZE + 24);
                 iov.buf = req.create_context;
                 iov.len = req.create_context_length;
-                smb2_set_uint32(&iov, 0, 0);    /* chain offset */
-                smb2_set_uint16(&iov, 4, 16);   /* tag offset */
-                smb2_set_uint16(&iov, 6, 4);    /* tag length lo */
-                smb2_set_uint16(&iov, 8, 0);    /* tag length up */
-                smb2_set_uint16(&iov, 10, 24);  /* data offset */
+                smb2_set_uint32(&iov, 0, 0);   /* chain offset */
+                smb2_set_uint16(&iov, 4, 16);  /* tag offset */
+                smb2_set_uint16(&iov, 6, 4);   /* tag length lo */
+                smb2_set_uint16(&iov, 8, 0);   /* tag length up */
+                smb2_set_uint16(&iov, 10, 24); /* data offset */
                 smb2_set_uint16(&iov, 12, SMB2_CREATE_REQUEST_LEASE_SIZE);
                 smb2_set_uint32(&iov, 16, htobe32(0x52714c73));
                 memcpy(iov.buf + 24, lease_key, SMB2_LEASE_KEY_SIZE);
@@ -1381,50 +1267,43 @@ _smb2_open_async_with_oplock_or_lease(struct smb2_context *smb2, const char *pat
         return pdu;
 }
 
-struct smb2_pdu *
-smb2_open_async_pdu(struct smb2_context *smb2, const char *path, int flags,
-                    smb2_command_cb cb, void *cb_data, void (*free_cb)(void *))
+struct smb2_pdu *smb2_open_async_pdu(struct smb2_context *smb2, const char *path, int flags,
+                                     smb2_command_cb cb, void *cb_data, void (*free_cb)(void *))
 {
-        return _smb2_open_async_with_oplock_or_lease(smb2, path, flags,
-                SMB2_OPLOCK_LEVEL_NONE, 0, NULL,
-                cb, cb_data, free_cb, 1);
+        return _smb2_open_async_with_oplock_or_lease(smb2, path, flags, SMB2_OPLOCK_LEVEL_NONE, 0,
+                                                     NULL, cb, cb_data, free_cb, 1);
 }
 
-int
-smb2_open_async_with_oplock_or_lease(struct smb2_context *smb2, const char *path, int flags,
-                uint8_t oplock_level, uint32_t lease_state, smb2_lease_key lease_key,
-                smb2_command_cb cb, void *cb_data)
+int smb2_open_async_with_oplock_or_lease(struct smb2_context *smb2, const char *path, int flags,
+                                         uint8_t oplock_level, uint32_t lease_state,
+                                         smb2_lease_key lease_key, smb2_command_cb cb,
+                                         void *cb_data)
 
 {
         struct smb2_pdu *pdu;
 
-        pdu = _smb2_open_async_with_oplock_or_lease(smb2, path, flags,
-                oplock_level, lease_state, lease_key,
-                cb, cb_data, NULL, 0);
+        pdu = _smb2_open_async_with_oplock_or_lease(smb2, path, flags, oplock_level, lease_state,
+                                                    lease_key, cb, cb_data, NULL, 0);
         return pdu ? 0 : -1;
 }
 
-int
-smb2_open_async(struct smb2_context *smb2, const char *path, int flags,
-                smb2_command_cb cb, void *cb_data)
+int smb2_open_async(struct smb2_context *smb2, const char *path, int flags, smb2_command_cb cb,
+                    void *cb_data)
 {
         struct smb2_pdu *pdu;
 
-        pdu = _smb2_open_async_with_oplock_or_lease(smb2, path, flags,
-                SMB2_OPLOCK_LEVEL_NONE, 0, NULL,
-                cb, cb_data, NULL, 0);
+        pdu = _smb2_open_async_with_oplock_or_lease(smb2, path, flags, SMB2_OPLOCK_LEVEL_NONE, 0,
+                                                    NULL, cb, cb_data, NULL, 0);
         return pdu ? 0 : -1;
 }
 
-static void
-close_cb(struct smb2_context *smb2, int status,
-         void *command_data, void *private_data)
+static void close_cb(struct smb2_context *smb2, int status, void *command_data, void *private_data)
 {
         struct smb2fh *fh = private_data;
 
         if (status != SMB2_STATUS_SUCCESS) {
-                smb2_set_nterror(smb2, status, "Close failed with (0x%08x) %s",
-                               status, nterror_to_str(status));
+                smb2_set_nterror(smb2, status, "Close failed with (0x%08x) %s", status,
+                                 nterror_to_str(status));
                 fh->cb(smb2, -nterror_to_errno(status), NULL, fh->cb_data);
                 free_smb2fh(smb2, fh);
                 return;
@@ -1434,19 +1313,18 @@ close_cb(struct smb2_context *smb2, int status,
         free_smb2fh(smb2, fh);
 }
 
-int
-smb2_close_async(struct smb2_context *smb2, struct smb2fh *fh,
-                 smb2_command_cb cb, void *cb_data)
+int smb2_close_async(struct smb2_context *smb2, struct smb2fh *fh, smb2_command_cb cb,
+                     void *cb_data)
 {
         struct smb2_close_request req;
         struct smb2_pdu *pdu;
 
         if (smb2 == NULL) {
-            return -EINVAL;
+                return -EINVAL;
         }
         if (fh == NULL) {
-            smb2_set_error(smb2, "File handle was NULL");
-            return -EINVAL;
+                smb2_set_error(smb2, "File handle was NULL");
+                return -EINVAL;
         }
 
         fh->cb = cb;
@@ -1466,15 +1344,13 @@ smb2_close_async(struct smb2_context *smb2, struct smb2fh *fh,
         return 0;
 }
 
-static void
-fsync_cb(struct smb2_context *smb2, int status,
-         void *command_data, void *private_data)
+static void fsync_cb(struct smb2_context *smb2, int status, void *command_data, void *private_data)
 {
         struct smb2fh *fh = private_data;
 
         if (status != SMB2_STATUS_SUCCESS) {
-                smb2_set_nterror(smb2, status, "Flush failed with (0x%08x) %s",
-                               status, nterror_to_str(status));
+                smb2_set_nterror(smb2, status, "Flush failed with (0x%08x) %s", status,
+                                 nterror_to_str(status));
                 fh->cb(smb2, -nterror_to_errno(status), NULL, fh->cb_data);
                 return;
         }
@@ -1482,19 +1358,18 @@ fsync_cb(struct smb2_context *smb2, int status,
         fh->cb(smb2, 0, NULL, fh->cb_data);
 }
 
-int
-smb2_fsync_async(struct smb2_context *smb2, struct smb2fh *fh,
-                 smb2_command_cb cb, void *cb_data)
+int smb2_fsync_async(struct smb2_context *smb2, struct smb2fh *fh, smb2_command_cb cb,
+                     void *cb_data)
 {
         struct smb2_flush_request req;
         struct smb2_pdu *pdu;
 
         if (smb2 == NULL) {
-            return -EINVAL;
+                return -EINVAL;
         }
         if (fh == NULL) {
-            smb2_set_error(smb2, "File handle was NULL");
-            return -EINVAL;
+                smb2_set_error(smb2, "File handle was NULL");
+                return -EINVAL;
         }
 
         fh->cb = cb;
@@ -1520,16 +1395,14 @@ struct read_data {
         struct smb2_read_cb_data read_cb_data;
 };
 
-static void
-read_cb(struct smb2_context *smb2, int status,
-      void *command_data, void *private_data)
+static void read_cb(struct smb2_context *smb2, int status, void *command_data, void *private_data)
 {
         struct read_data *rd = private_data;
         struct smb2_read_reply *rep = command_data;
 
         if (status && status != SMB2_STATUS_END_OF_FILE) {
-                smb2_set_nterror(smb2, status, "Read/Write failed with (0x%08x) %s",
-                               status, nterror_to_str(status));
+                smb2_set_nterror(smb2, status, "Read/Write failed with (0x%08x) %s", status,
+                                 nterror_to_str(status));
                 rd->cb(smb2, -nterror_to_errno(status), &rd->read_cb_data, rd->cb_data);
                 free(rd);
                 return;
@@ -1543,10 +1416,8 @@ read_cb(struct smb2_context *smb2, int status,
         free(rd);
 }
 
-int
-smb2_pread_async(struct smb2_context *smb2, struct smb2fh *fh,
-                 uint8_t *buf, uint32_t count, uint64_t offset,
-                 smb2_command_cb cb, void *cb_data)
+int smb2_pread_async(struct smb2_context *smb2, struct smb2fh *fh, uint8_t *buf, uint32_t count,
+                     uint64_t offset, smb2_command_cb cb, void *cb_data)
 {
         struct smb2_read_request req;
         struct read_data *rd;
@@ -1581,7 +1452,7 @@ smb2_pread_async(struct smb2_context *smb2, struct smb2fh *fh,
 
         if (smb2->dialect > SMB2_VERSION_0202) {
                 if (needed_credits > MAX_CREDITS - 16) {
-                        count =  (MAX_CREDITS - 16) * 65536;
+                        count = (MAX_CREDITS - 16) * 65536;
                 }
                 needed_credits = (count - 1) / 65536 + 1;
                 if (needed_credits > smb2->credits) {
@@ -1615,10 +1486,8 @@ smb2_pread_async(struct smb2_context *smb2, struct smb2fh *fh,
         return 0;
 }
 
-int
-smb2_read_async(struct smb2_context *smb2, struct smb2fh *fh,
-                uint8_t *buf, uint32_t count,
-                smb2_command_cb cb, void *cb_data)
+int smb2_read_async(struct smb2_context *smb2, struct smb2fh *fh, uint8_t *buf, uint32_t count,
+                    smb2_command_cb cb, void *cb_data)
 {
         if (smb2 == NULL) {
                 return -EINVAL;
@@ -1628,8 +1497,7 @@ smb2_read_async(struct smb2_context *smb2, struct smb2fh *fh,
                 return -EINVAL;
         }
 
-        return smb2_pread_async(smb2, fh, buf, count, fh->offset,
-                                cb, cb_data);
+        return smb2_pread_async(smb2, fh, buf, count, fh->offset, cb, cb_data);
 }
 
 struct write_data {
@@ -1639,16 +1507,14 @@ struct write_data {
         struct smb2_write_cb_data write_cb_data;
 };
 
-static void
-write_cb(struct smb2_context *smb2, int status,
-      void *command_data, void *private_data)
+static void write_cb(struct smb2_context *smb2, int status, void *command_data, void *private_data)
 {
         struct write_data *wd = private_data;
         struct smb2_write_reply *rep = command_data;
 
         if (status && status != SMB2_STATUS_END_OF_FILE) {
-                smb2_set_nterror(smb2, status, "Read/Write failed with (0x%08x) %s",
-                               status, nterror_to_str(status));
+                smb2_set_nterror(smb2, status, "Read/Write failed with (0x%08x) %s", status,
+                                 nterror_to_str(status));
                 wd->cb(smb2, -nterror_to_errno(status), &wd->write_cb_data, wd->cb_data);
                 free(wd);
                 return;
@@ -1662,10 +1528,8 @@ write_cb(struct smb2_context *smb2, int status,
         free(wd);
 }
 
-int
-smb2_pwrite_async(struct smb2_context *smb2, struct smb2fh *fh,
-                  const uint8_t *buf, uint32_t count, uint64_t offset,
-                  smb2_command_cb cb, void *cb_data)
+int smb2_pwrite_async(struct smb2_context *smb2, struct smb2fh *fh, const uint8_t *buf,
+                      uint32_t count, uint64_t offset, smb2_command_cb cb, void *cb_data)
 {
         struct smb2_write_request req;
         struct write_data *wr;
@@ -1700,7 +1564,7 @@ smb2_pwrite_async(struct smb2_context *smb2, struct smb2fh *fh,
 
         if (smb2->dialect > SMB2_VERSION_0202) {
                 if (needed_credits > MAX_CREDITS - 16) {
-                        count =  (MAX_CREDITS - 16) * 65536;
+                        count = (MAX_CREDITS - 16) * 65536;
                 }
                 needed_credits = (count - 1) / 65536 + 1;
                 if (needed_credits > smb2->credits) {
@@ -1732,10 +1596,8 @@ smb2_pwrite_async(struct smb2_context *smb2, struct smb2fh *fh,
         return 0;
 }
 
-int
-smb2_write_async(struct smb2_context *smb2, struct smb2fh *fh,
-                 const uint8_t *buf, uint32_t count,
-                 smb2_command_cb cb, void *cb_data)
+int smb2_write_async(struct smb2_context *smb2, struct smb2fh *fh, const uint8_t *buf,
+                     uint32_t count, smb2_command_cb cb, void *cb_data)
 {
         if (smb2 == NULL) {
                 return -EINVAL;
@@ -1744,13 +1606,11 @@ smb2_write_async(struct smb2_context *smb2, struct smb2fh *fh,
                 smb2_set_error(smb2, "File handle was NULL");
                 return -EINVAL;
         }
-        return smb2_pwrite_async(smb2, fh, buf, count, fh->offset,
-                                 cb, cb_data);
+        return smb2_pwrite_async(smb2, fh, buf, count, fh->offset, cb, cb_data);
 }
 
-int64_t
-smb2_lseek(struct smb2_context *smb2, struct smb2fh *fh,
-           int64_t offset, int whence, uint64_t *current_offset)
+int64_t smb2_lseek(struct smb2_context *smb2, struct smb2fh *fh, int64_t offset, int whence,
+                   uint64_t *current_offset)
 {
         if (smb2 == NULL) {
                 return -EINVAL;
@@ -1760,11 +1620,11 @@ smb2_lseek(struct smb2_context *smb2, struct smb2fh *fh,
                 return -EINVAL;
         }
 
-        switch(whence) {
+        switch (whence) {
         case SEEK_SET:
                 if (offset < 0) {
                         smb2_set_error(smb2, "Lseek() offset would become"
-                                        "negative");
+                                             "negative");
                         return -EINVAL;
                 }
                 fh->offset = offset;
@@ -1775,7 +1635,7 @@ smb2_lseek(struct smb2_context *smb2, struct smb2fh *fh,
         case SEEK_CUR:
                 if (fh->offset + offset < 0) {
                         smb2_set_error(smb2, "Lseek() offset would become"
-                                        "negative");
+                                             "negative");
                         return -EINVAL;
                 }
                 fh->offset += offset;
@@ -1787,7 +1647,7 @@ smb2_lseek(struct smb2_context *smb2, struct smb2fh *fh,
                 fh->offset = fh->end_of_file;
                 if (fh->offset + offset < 0) {
                         smb2_set_error(smb2, "Lseek() offset would become"
-                                        "negative");
+                                             "negative");
                         return -EINVAL;
                 }
                 fh->offset += offset;
@@ -1796,8 +1656,7 @@ smb2_lseek(struct smb2_context *smb2, struct smb2fh *fh,
                 }
                 return fh->offset;
         default:
-                smb2_set_error(smb2, "Invalid whence(%d) for lseek",
-                                    whence);
+                smb2_set_error(smb2, "Invalid whence(%d) for lseek", whence);
                 return -EINVAL;
         }
 }
@@ -1807,9 +1666,8 @@ struct create_cb_data {
         void *cb_data;
 };
 
-static void
-create_cb_2(struct smb2_context *smb2, int status,
-            void *command_data, void *private_data)
+static void create_cb_2(struct smb2_context *smb2, int status, void *command_data,
+                        void *private_data)
 {
         struct create_cb_data *create_data = private_data;
 
@@ -1821,9 +1679,8 @@ create_cb_2(struct smb2_context *smb2, int status,
         free(create_data);
 }
 
-static void
-create_cb_1(struct smb2_context *smb2, int status,
-            void *command_data, void *private_data)
+static void create_cb_1(struct smb2_context *smb2, int status, void *command_data,
+                        void *private_data)
 {
         if (status != SMB2_STATUS_SUCCESS) {
                 smb2_set_error(smb2, "Create failed with status %s.", nterror_to_str(status));
@@ -1831,10 +1688,8 @@ create_cb_1(struct smb2_context *smb2, int status,
         }
 }
 
-static int
-smb2_unlink_internal(struct smb2_context *smb2, const char *path,
-                     int is_dir,
-                     smb2_command_cb cb, void *cb_data)
+static int smb2_unlink_internal(struct smb2_context *smb2, const char *path, int is_dir,
+                                smb2_command_cb cb, void *cb_data)
 {
         struct create_cb_data *create_data;
         struct smb2_create_request cr_req;
@@ -1854,7 +1709,6 @@ smb2_unlink_internal(struct smb2_context *smb2, const char *path,
         create_data->cb = cb;
         create_data->cb_data = cb_data;
 
-
         memset(&cr_req, 0, sizeof(struct smb2_create_request));
         cr_req.requested_oplock_level = SMB2_OPLOCK_LEVEL_NONE;
         cr_req.impersonation_level = SMB2_IMPERSONATION_IMPERSONATION;
@@ -1864,8 +1718,7 @@ smb2_unlink_internal(struct smb2_context *smb2, const char *path,
         } else {
                 cr_req.file_attributes = SMB2_FILE_ATTRIBUTE_NORMAL;
         }
-        cr_req.share_access = SMB2_FILE_SHARE_READ | SMB2_FILE_SHARE_WRITE |
-                SMB2_FILE_SHARE_DELETE;
+        cr_req.share_access = SMB2_FILE_SHARE_READ | SMB2_FILE_SHARE_WRITE | SMB2_FILE_SHARE_DELETE;
         cr_req.create_disposition = SMB2_FILE_OPEN;
         cr_req.create_options = SMB2_FILE_DELETE_ON_CLOSE;
         cr_req.name = path;
@@ -1894,23 +1747,18 @@ smb2_unlink_internal(struct smb2_context *smb2, const char *path,
         return 0;
 }
 
-int
-smb2_unlink_async(struct smb2_context *smb2, const char *path,
-                  smb2_command_cb cb, void *cb_data)
+int smb2_unlink_async(struct smb2_context *smb2, const char *path, smb2_command_cb cb,
+                      void *cb_data)
 {
         return smb2_unlink_internal(smb2, path, 0, cb, cb_data);
 }
 
-int
-smb2_rmdir_async(struct smb2_context *smb2, const char *path,
-                 smb2_command_cb cb, void *cb_data)
+int smb2_rmdir_async(struct smb2_context *smb2, const char *path, smb2_command_cb cb, void *cb_data)
 {
         return smb2_unlink_internal(smb2, path, 1, cb, cb_data);
 }
 
-int
-smb2_mkdir_async(struct smb2_context *smb2, const char *path,
-                 smb2_command_cb cb, void *cb_data)
+int smb2_mkdir_async(struct smb2_context *smb2, const char *path, smb2_command_cb cb, void *cb_data)
 {
         struct create_cb_data *create_data;
         struct smb2_create_request cr_req;
@@ -1974,9 +1822,8 @@ struct stat_cb_data {
         void *st;
 };
 
-static void
-fstat_cb_1(struct smb2_context *smb2, int status,
-           void *command_data, void *private_data)
+static void fstat_cb_1(struct smb2_context *smb2, int status, void *command_data,
+                       void *private_data)
 {
         struct stat_cb_data *stat_data = private_data;
         struct smb2_query_info_reply *rep = command_data;
@@ -1984,8 +1831,7 @@ fstat_cb_1(struct smb2_context *smb2, int status,
         struct smb2_stat_64 *st = stat_data->st;
 
         if (status != SMB2_STATUS_SUCCESS) {
-                stat_data->cb(smb2, -nterror_to_errno(status),
-                       NULL, stat_data->cb_data);
+                stat_data->cb(smb2, -nterror_to_errno(status), NULL, stat_data->cb_data);
                 free(stat_data);
                 return;
         }
@@ -1997,21 +1843,17 @@ fstat_cb_1(struct smb2_context *smb2, int status,
         if (fs->basic.file_attributes & SMB2_FILE_ATTRIBUTE_REPARSE_POINT) {
                 st->smb2_type = SMB2_TYPE_LINK;
         }
-        st->smb2_nlink      = fs->standard.number_of_links;
-        st->smb2_ino        = fs->index_number;
-        st->smb2_size       = fs->standard.end_of_file;
-        st->smb2_atime      = fs->basic.last_access_time.tv_sec;
-        st->smb2_atime_nsec = fs->basic.last_access_time.tv_usec *
-                1000;
-        st->smb2_mtime      = fs->basic.last_write_time.tv_sec;
-        st->smb2_mtime_nsec = fs->basic.last_write_time.tv_usec *
-                1000;
-        st->smb2_ctime      = fs->basic.change_time.tv_sec;
-        st->smb2_ctime_nsec = fs->basic.change_time.tv_usec *
-                1000;
-        st->smb2_btime      = fs->basic.creation_time.tv_sec;
-        st->smb2_btime_nsec = fs->basic.creation_time.tv_usec *
-                1000;
+        st->smb2_nlink = fs->standard.number_of_links;
+        st->smb2_ino = fs->index_number;
+        st->smb2_size = fs->standard.end_of_file;
+        st->smb2_atime = fs->basic.last_access_time.tv_sec;
+        st->smb2_atime_nsec = fs->basic.last_access_time.tv_usec * 1000;
+        st->smb2_mtime = fs->basic.last_write_time.tv_sec;
+        st->smb2_mtime_nsec = fs->basic.last_write_time.tv_usec * 1000;
+        st->smb2_ctime = fs->basic.change_time.tv_sec;
+        st->smb2_ctime_nsec = fs->basic.change_time.tv_usec * 1000;
+        st->smb2_btime = fs->basic.creation_time.tv_sec;
+        st->smb2_btime_nsec = fs->basic.creation_time.tv_usec * 1000;
 
         smb2_free_data(smb2, fs);
 
@@ -2019,10 +1861,8 @@ fstat_cb_1(struct smb2_context *smb2, int status,
         free(stat_data);
 }
 
-int
-smb2_fstat_async(struct smb2_context *smb2, struct smb2fh *fh,
-                 struct smb2_stat_64 *st,
-                 smb2_command_cb cb, void *cb_data)
+int smb2_fstat_async(struct smb2_context *smb2, struct smb2fh *fh, struct smb2_stat_64 *st,
+                     smb2_command_cb cb, void *cb_data)
 {
         struct stat_cb_data *stat_data;
         struct smb2_query_info_request req;
@@ -2065,9 +1905,8 @@ smb2_fstat_async(struct smb2_context *smb2, struct smb2fh *fh,
         return 0;
 }
 
-static void
-getinfo_cb_3(struct smb2_context *smb2, int status,
-             void *command_data _U_, void *private_data)
+static void getinfo_cb_3(struct smb2_context *smb2, int status, void *command_data _U_,
+                         void *private_data)
 {
         struct stat_cb_data *stat_data = private_data;
 
@@ -2075,14 +1914,13 @@ getinfo_cb_3(struct smb2_context *smb2, int status,
                 stat_data->status = status;
         }
 
-        stat_data->cb(smb2, -nterror_to_errno(stat_data->status),
-                      stat_data->st, stat_data->cb_data);
+        stat_data->cb(smb2, -nterror_to_errno(stat_data->status), stat_data->st,
+                      stat_data->cb_data);
         free(stat_data);
 }
 
-static void
-getinfo_cb_2(struct smb2_context *smb2, int status,
-             void *command_data, void *private_data)
+static void getinfo_cb_2(struct smb2_context *smb2, int status, void *command_data,
+                         void *private_data)
 {
         struct stat_cb_data *stat_data = private_data;
         struct smb2_query_info_reply *rep = command_data;
@@ -2106,21 +1944,17 @@ getinfo_cb_2(struct smb2_context *smb2, int status,
                 if (fs->basic.file_attributes & SMB2_FILE_ATTRIBUTE_REPARSE_POINT) {
                         st->smb2_type = SMB2_TYPE_LINK;
                 }
-                st->smb2_nlink      = fs->standard.number_of_links;
-                st->smb2_ino        = fs->index_number;
-                st->smb2_size       = fs->standard.end_of_file;
-                st->smb2_atime      = fs->basic.last_access_time.tv_sec;
-                st->smb2_atime_nsec = fs->basic.last_access_time.tv_usec *
-                        1000;
-                st->smb2_mtime      = fs->basic.last_write_time.tv_sec;
-                st->smb2_mtime_nsec = fs->basic.last_write_time.tv_usec *
-                        1000;
-                st->smb2_ctime      = fs->basic.change_time.tv_sec;
-                st->smb2_ctime_nsec = fs->basic.change_time.tv_usec *
-                        1000;
-                st->smb2_btime      = fs->basic.creation_time.tv_sec;
-                st->smb2_btime_nsec = fs->basic.creation_time.tv_usec *
-                        1000;
+                st->smb2_nlink = fs->standard.number_of_links;
+                st->smb2_ino = fs->index_number;
+                st->smb2_size = fs->standard.end_of_file;
+                st->smb2_atime = fs->basic.last_access_time.tv_sec;
+                st->smb2_atime_nsec = fs->basic.last_access_time.tv_usec * 1000;
+                st->smb2_mtime = fs->basic.last_write_time.tv_sec;
+                st->smb2_mtime_nsec = fs->basic.last_write_time.tv_usec * 1000;
+                st->smb2_ctime = fs->basic.change_time.tv_sec;
+                st->smb2_ctime_nsec = fs->basic.change_time.tv_usec * 1000;
+                st->smb2_btime = fs->basic.creation_time.tv_sec;
+                st->smb2_btime_nsec = fs->basic.creation_time.tv_usec * 1000;
         } else if (stat_data->info_type == SMB2_0_INFO_FILESYSTEM &&
                    stat_data->file_info_class == SMB2_FILE_FS_FULL_SIZE_INFORMATION) {
                 struct smb2_statvfs *statvfs = stat_data->st;
@@ -2128,18 +1962,15 @@ getinfo_cb_2(struct smb2_context *smb2, int status,
 
                 memset(statvfs, 0, sizeof(struct smb2_statvfs));
                 statvfs->f_bsize = statvfs->f_frsize =
-                        vfs->bytes_per_sector *
-                        vfs->sectors_per_allocation_unit;
+                    vfs->bytes_per_sector * vfs->sectors_per_allocation_unit;
                 statvfs->f_blocks = vfs->total_allocation_units;
-                statvfs->f_bfree = statvfs->f_bavail =
-                        vfs->caller_available_allocation_units;
+                statvfs->f_bfree = statvfs->f_bavail = vfs->caller_available_allocation_units;
         }
         smb2_free_data(smb2, rep->output_buffer);
 }
 
-static void
-getinfo_cb_1(struct smb2_context *smb2, int status,
-             void *command_data _U_, void *private_data)
+static void getinfo_cb_1(struct smb2_context *smb2, int status, void *command_data _U_,
+                         void *private_data)
 {
         struct stat_cb_data *stat_data = private_data;
 
@@ -2148,11 +1979,8 @@ getinfo_cb_1(struct smb2_context *smb2, int status,
         }
 }
 
-static int
-smb2_getinfo_async(struct smb2_context *smb2, const char *path,
-                   uint8_t info_type, uint8_t file_info_class,
-                   void *st,
-                   smb2_command_cb cb, void *cb_data)
+static int smb2_getinfo_async(struct smb2_context *smb2, const char *path, uint8_t info_type,
+                              uint8_t file_info_class, void *st, smb2_command_cb cb, void *cb_data)
 {
         struct stat_cb_data *stat_data;
         struct smb2_create_request cr_req;
@@ -2203,8 +2031,7 @@ smb2_getinfo_async(struct smb2_context *smb2, const char *path,
         qi_req.flags = 0;
         memcpy(qi_req.file_id, compound_file_id, SMB2_FD_SIZE);
 
-        next_pdu = smb2_cmd_query_info_async(smb2, &qi_req,
-                                             getinfo_cb_2, stat_data);
+        next_pdu = smb2_cmd_query_info_async(smb2, &qi_req, getinfo_cb_2, stat_data);
         if (next_pdu == NULL) {
                 smb2_set_error(smb2, "Failed to create query command");
                 free(stat_data);
@@ -2232,26 +2059,18 @@ smb2_getinfo_async(struct smb2_context *smb2, const char *path,
         return 0;
 }
 
-int
-smb2_stat_async(struct smb2_context *smb2, const char *path,
-                struct smb2_stat_64 *st,
-                smb2_command_cb cb, void *cb_data)
+int smb2_stat_async(struct smb2_context *smb2, const char *path, struct smb2_stat_64 *st,
+                    smb2_command_cb cb, void *cb_data)
 {
-        return smb2_getinfo_async(smb2, path,
-                                  SMB2_0_INFO_FILE,
-                                  SMB2_FILE_ALL_INFORMATION,
-                                  st, cb, cb_data);
+        return smb2_getinfo_async(smb2, path, SMB2_0_INFO_FILE, SMB2_FILE_ALL_INFORMATION, st, cb,
+                                  cb_data);
 }
 
-int
-smb2_statvfs_async(struct smb2_context *smb2, const char *path,
-                   struct smb2_statvfs *statvfs,
-                   smb2_command_cb cb, void *cb_data)
+int smb2_statvfs_async(struct smb2_context *smb2, const char *path, struct smb2_statvfs *statvfs,
+                       smb2_command_cb cb, void *cb_data)
 {
-        return smb2_getinfo_async(smb2, path,
-                                  SMB2_0_INFO_FILESYSTEM,
-                                  SMB2_FILE_FS_FULL_SIZE_INFORMATION,
-                                  statvfs, cb, cb_data);
+        return smb2_getinfo_async(smb2, path, SMB2_0_INFO_FILESYSTEM,
+                                  SMB2_FILE_FS_FULL_SIZE_INFORMATION, statvfs, cb, cb_data);
 }
 
 struct trunc_cb_data {
@@ -2262,9 +2081,8 @@ struct trunc_cb_data {
         uint64_t length;
 };
 
-static void
-trunc_cb_3(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void trunc_cb_3(struct smb2_context *smb2, int status, void *command_data _U_,
+                       void *private_data)
 {
         struct trunc_cb_data *trunc_data = private_data;
 
@@ -2272,14 +2090,12 @@ trunc_cb_3(struct smb2_context *smb2, int status,
                 trunc_data->status = status;
         }
 
-        trunc_data->cb(smb2, -nterror_to_errno(trunc_data->status),
-                       NULL, trunc_data->cb_data);
+        trunc_data->cb(smb2, -nterror_to_errno(trunc_data->status), NULL, trunc_data->cb_data);
         free(trunc_data);
 }
 
-static void
-trunc_cb_2(struct smb2_context *smb2, int status,
-           void *command_data, void *private_data)
+static void trunc_cb_2(struct smb2_context *smb2, int status, void *command_data,
+                       void *private_data)
 {
         struct trunc_cb_data *trunc_data = private_data;
 
@@ -2288,9 +2104,8 @@ trunc_cb_2(struct smb2_context *smb2, int status,
         }
 }
 
-static void
-trunc_cb_1(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void trunc_cb_1(struct smb2_context *smb2, int status, void *command_data _U_,
+                       void *private_data)
 {
         struct trunc_cb_data *trunc_data = private_data;
 
@@ -2299,9 +2114,8 @@ trunc_cb_1(struct smb2_context *smb2, int status,
         }
 }
 
-int
-smb2_truncate_async(struct smb2_context *smb2, const char *path,
-                    uint64_t length, smb2_command_cb cb, void *cb_data)
+int smb2_truncate_async(struct smb2_context *smb2, const char *path, uint64_t length,
+                        smb2_command_cb cb, void *cb_data)
 {
         struct trunc_cb_data *trunc_data;
         struct smb2_create_request cr_req;
@@ -2352,11 +2166,9 @@ smb2_truncate_async(struct smb2_context *smb2, const char *path,
         memcpy(si_req.file_id, compound_file_id, SMB2_FD_SIZE);
         si_req.input_data = &eofi;
 
-        next_pdu = smb2_cmd_set_info_async(smb2, &si_req,
-                                           trunc_cb_2, trunc_data);
+        next_pdu = smb2_cmd_set_info_async(smb2, &si_req, trunc_cb_2, trunc_data);
         if (next_pdu == NULL) {
-                smb2_set_error(smb2, "Failed to create set command. %s",
-                               smb2_get_error(smb2));
+                smb2_set_error(smb2, "Failed to create set command. %s", smb2_get_error(smb2));
                 free(trunc_data);
                 smb2_free_pdu(smb2, pdu);
                 return -EINVAL;
@@ -2396,9 +2208,8 @@ static void free_new_name_data(struct new_name_cb_data *new_name_data)
         free(new_name_data);
 }
 
-static void
-new_name_cb_3(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void new_name_cb_3(struct smb2_context *smb2, int status, void *command_data _U_,
+                          void *private_data)
 {
         struct new_name_cb_data *new_name_data = private_data;
 
@@ -2406,14 +2217,13 @@ new_name_cb_3(struct smb2_context *smb2, int status,
                 new_name_data->status = status;
         }
 
-        new_name_data->cb(smb2, -nterror_to_errno(new_name_data->status),
-                        NULL, new_name_data->cb_data);
+        new_name_data->cb(smb2, -nterror_to_errno(new_name_data->status), NULL,
+                          new_name_data->cb_data);
         free_new_name_data(new_name_data);
 }
 
-static void
-new_name_cb_2(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void new_name_cb_2(struct smb2_context *smb2, int status, void *command_data _U_,
+                          void *private_data)
 {
         struct new_name_cb_data *new_name_data = private_data;
 
@@ -2422,9 +2232,8 @@ new_name_cb_2(struct smb2_context *smb2, int status,
         }
 }
 
-static void
-new_name_cb_1(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void new_name_cb_1(struct smb2_context *smb2, int status, void *command_data _U_,
+                          void *private_data)
 {
         struct new_name_cb_data *new_name_data = private_data;
 
@@ -2433,11 +2242,9 @@ new_name_cb_1(struct smb2_context *smb2, int status,
         }
 }
 
-static int
-smb2_new_name_async(struct smb2_context *smb2, const char *oldpath,
-                    const char *newpath, uint8_t file_info_class,
-                    uint32_t desired_access,
-                    smb2_command_cb cb, void *cb_data)
+static int smb2_new_name_async(struct smb2_context *smb2, const char *oldpath, const char *newpath,
+                               uint8_t file_info_class, uint32_t desired_access, smb2_command_cb cb,
+                               void *cb_data)
 {
         struct new_name_cb_data *new_name_data;
         struct smb2_create_request cr_req;
@@ -2506,11 +2313,9 @@ smb2_new_name_async(struct smb2_context *smb2, const char *oldpath,
                 si_req.input_data = &rn_info;
         }
 
-        next_pdu = smb2_cmd_set_info_async(smb2, &si_req,
-                                           new_name_cb_2, new_name_data);
+        next_pdu = smb2_cmd_set_info_async(smb2, &si_req, new_name_cb_2, new_name_data);
         if (next_pdu == NULL) {
-                smb2_set_error(smb2, "Failed to create set command. %s",
-                               smb2_get_error(smb2));
+                smb2_set_error(smb2, "Failed to create set command. %s", smb2_get_error(smb2));
                 free_new_name_data(new_name_data);
                 smb2_free_pdu(smb2, pdu);
                 return -EINVAL;
@@ -2536,44 +2341,34 @@ smb2_new_name_async(struct smb2_context *smb2, const char *oldpath,
         return 0;
 }
 
-int
-smb2_rename_async(struct smb2_context *smb2, const char *oldpath,
-                  const char *newpath, smb2_command_cb cb, void *cb_data)
+int smb2_rename_async(struct smb2_context *smb2, const char *oldpath, const char *newpath,
+                      smb2_command_cb cb, void *cb_data)
 {
-        return smb2_new_name_async(smb2, oldpath, newpath,
-                                   SMB2_FILE_RENAME_INFORMATION,
-                                   SMB2_GENERIC_READ |
-                                   SMB2_FILE_READ_ATTRIBUTES |
-                                   SMB2_DELETE,
-                                   cb, cb_data);
+        return smb2_new_name_async(smb2, oldpath, newpath, SMB2_FILE_RENAME_INFORMATION,
+                                   SMB2_GENERIC_READ | SMB2_FILE_READ_ATTRIBUTES | SMB2_DELETE, cb,
+                                   cb_data);
 }
 
-int
-smb2_link_async(struct smb2_context *smb2, const char *oldpath,
-                const char *newpath, smb2_command_cb cb, void *cb_data)
+int smb2_link_async(struct smb2_context *smb2, const char *oldpath, const char *newpath,
+                    smb2_command_cb cb, void *cb_data)
 {
         /* Unlike rename, creating a hard link does not need SMB2_DELETE
          * access to the source file. */
-        return smb2_new_name_async(smb2, oldpath, newpath,
-                                   SMB2_FILE_LINK_INFORMATION,
-                                   SMB2_FILE_READ_ATTRIBUTES,
-                                   cb, cb_data);
+        return smb2_new_name_async(smb2, oldpath, newpath, SMB2_FILE_LINK_INFORMATION,
+                                   SMB2_FILE_READ_ATTRIBUTES, cb, cb_data);
 }
 
-static void
-ftrunc_cb_1(struct smb2_context *smb2, int status,
-            void *command_data _U_, void *private_data)
+static void ftrunc_cb_1(struct smb2_context *smb2, int status, void *command_data _U_,
+                        void *private_data)
 {
         struct create_cb_data *cb_data = private_data;
 
-        cb_data->cb(smb2, -nterror_to_errno(status),
-                    NULL, cb_data->cb_data);
+        cb_data->cb(smb2, -nterror_to_errno(status), NULL, cb_data->cb_data);
         free(cb_data);
 }
 
-int
-smb2_ftruncate_async(struct smb2_context *smb2, struct smb2fh *fh,
-                     uint64_t length, smb2_command_cb cb, void *cb_data)
+int smb2_ftruncate_async(struct smb2_context *smb2, struct smb2fh *fh, uint64_t length,
+                         smb2_command_cb cb, void *cb_data)
 {
         struct create_cb_data *create_data;
         struct smb2_set_info_request req;
@@ -2624,13 +2419,12 @@ struct readlink_cb_data {
         struct smb2_reparse_data_buffer *reparse;
 };
 
-static void
-readlink_cb_3(struct smb2_context *smb2, int status,
-            void *command_data _U_, void *private_data)
+static void readlink_cb_3(struct smb2_context *smb2, int status, void *command_data _U_,
+                          void *private_data)
 {
         struct readlink_cb_data *cb_data = private_data;
         struct smb2_reparse_data_buffer *rp = cb_data->reparse;
-        char *target = (char*)"<unknown reparse point type>";
+        char *target = (char *)"<unknown reparse point type>";
 
         if (rp) {
                 switch (rp->reparse_tag) {
@@ -2638,15 +2432,13 @@ readlink_cb_3(struct smb2_context *smb2, int status,
                         target = rp->symlink.subname;
                 }
         }
-        cb_data->cb(smb2, -nterror_to_errno(cb_data->status),
-                    target, cb_data->cb_data);
+        cb_data->cb(smb2, -nterror_to_errno(cb_data->status), target, cb_data->cb_data);
         smb2_free_data(smb2, rp);
         free(cb_data);
 }
 
-static void
-readlink_cb_2(struct smb2_context *smb2, int status,
-            void *command_data, void *private_data)
+static void readlink_cb_2(struct smb2_context *smb2, int status, void *command_data,
+                          void *private_data)
 {
         struct readlink_cb_data *cb_data = private_data;
         struct smb2_ioctl_reply *rep = command_data;
@@ -2662,9 +2454,8 @@ readlink_cb_2(struct smb2_context *smb2, int status,
         }
 }
 
-static void
-readlink_cb_1(struct smb2_context *smb2, int status,
-            void *command_data _U_, void *private_data)
+static void readlink_cb_1(struct smb2_context *smb2, int status, void *command_data _U_,
+                          void *private_data)
 {
         struct readlink_cb_data *cb_data = private_data;
 
@@ -2674,9 +2465,8 @@ readlink_cb_1(struct smb2_context *smb2, int status,
         cb_data->status = status;
 }
 
-int
-smb2_readlink_async(struct smb2_context *smb2, const char *path,
-                    smb2_command_cb cb, void *cb_data)
+int smb2_readlink_async(struct smb2_context *smb2, const char *path, smb2_command_cb cb,
+                        void *cb_data)
 {
         struct readlink_cb_data *readlink_data;
         struct smb2_create_request cr_req;
@@ -2703,8 +2493,7 @@ smb2_readlink_async(struct smb2_context *smb2, const char *path,
         cr_req.impersonation_level = SMB2_IMPERSONATION_IMPERSONATION;
         cr_req.desired_access = SMB2_FILE_READ_ATTRIBUTES;
         cr_req.file_attributes = 0;
-        cr_req.share_access = SMB2_FILE_SHARE_READ | SMB2_FILE_SHARE_WRITE |
-                SMB2_FILE_SHARE_DELETE;
+        cr_req.share_access = SMB2_FILE_SHARE_READ | SMB2_FILE_SHARE_WRITE | SMB2_FILE_SHARE_DELETE;
         cr_req.create_disposition = SMB2_FILE_OPEN;
         cr_req.create_options = SMB2_FILE_OPEN_REPARSE_POINT;
         cr_req.name = path;
@@ -2724,8 +2513,7 @@ smb2_readlink_async(struct smb2_context *smb2, const char *path,
         io_req.input = NULL;
         io_req.flags = SMB2_0_IOCTL_IS_FSCTL;
 
-        next_pdu = smb2_cmd_ioctl_async(smb2, &io_req, readlink_cb_2,
-                                        readlink_data);
+        next_pdu = smb2_cmd_ioctl_async(smb2, &io_req, readlink_cb_2, readlink_data);
         if (next_pdu == NULL) {
                 free(readlink_data);
                 smb2_free_pdu(smb2, pdu);
@@ -2738,8 +2526,7 @@ smb2_readlink_async(struct smb2_context *smb2, const char *path,
         cl_req.flags = SMB2_CLOSE_FLAG_POSTQUERY_ATTRIB;
         memcpy(cl_req.file_id, compound_file_id, SMB2_FD_SIZE);
 
-        next_pdu = smb2_cmd_close_async(smb2, &cl_req, readlink_cb_3,
-                                        readlink_data);
+        next_pdu = smb2_cmd_close_async(smb2, &cl_req, readlink_cb_3, readlink_data);
         if (next_pdu == NULL) {
                 free(readlink_data);
                 smb2_free_pdu(smb2, pdu);
@@ -2759,9 +2546,8 @@ struct smb2_ioctl_cb_data {
         uint32_t ctl_code;
 };
 
-static void
-smb2_ioctl_output_cb(struct smb2_context *smb2, int status,
-                     void *command_data, void *private_data)
+static void smb2_ioctl_output_cb(struct smb2_context *smb2, int status, void *command_data,
+                                 void *private_data)
 {
         struct smb2_ioctl_cb_data *ioctl_data = private_data;
         void *output = NULL;
@@ -2788,19 +2574,15 @@ smb2_ioctl_output_cb(struct smb2_context *smb2, int status,
                 }
         }
 
-        ioctl_data->cb(smb2, -nterror_to_errno(status), output,
-                       ioctl_data->cb_data);
+        ioctl_data->cb(smb2, -nterror_to_errno(status), output, ioctl_data->cb_data);
         free(ioctl_data->input);
         free(ioctl_data);
 }
 
-static int
-smb2_encode_copychunk_input(struct smb2_context *smb2,
-                            const struct smb2_srv_copychunk_resume_key *resume_key,
-                            const struct smb2_srv_copychunk *chunks,
-                            uint32_t chunk_count,
-                            uint8_t **input,
-                            uint32_t *input_count)
+static int smb2_encode_copychunk_input(struct smb2_context *smb2,
+                                       const struct smb2_srv_copychunk_resume_key *resume_key,
+                                       const struct smb2_srv_copychunk *chunks,
+                                       uint32_t chunk_count, uint8_t **input, uint32_t *input_count)
 {
         struct smb2_iovec iov;
         uint32_t i;
@@ -2833,8 +2615,7 @@ smb2_encode_copychunk_input(struct smb2_context *smb2,
         iov.buf = *input;
         iov.len = *input_count;
 
-        memcpy(iov.buf, resume_key->resume_key,
-               SMB2_SRV_COPYCHUNK_RESUME_KEY_SIZE);
+        memcpy(iov.buf, resume_key->resume_key, SMB2_SRV_COPYCHUNK_RESUME_KEY_SIZE);
         smb2_set_uint32(&iov, 24, chunk_count);
 
         for (i = 0; i < chunk_count; i++) {
@@ -2849,14 +2630,11 @@ smb2_encode_copychunk_input(struct smb2_context *smb2,
         return 0;
 }
 
-static int
-smb2_copychunk_submit_async(struct smb2_context *smb2,
-                            uint32_t ctl_code,
-                            const struct smb2_srv_copychunk_resume_key *resume_key,
-                            struct smb2fh *dstfh,
-                            const struct smb2_srv_copychunk *chunks,
-                            uint32_t chunk_count,
-                            smb2_command_cb cb, void *cb_data)
+static int smb2_copychunk_submit_async(struct smb2_context *smb2, uint32_t ctl_code,
+                                       const struct smb2_srv_copychunk_resume_key *resume_key,
+                                       struct smb2fh *dstfh,
+                                       const struct smb2_srv_copychunk *chunks,
+                                       uint32_t chunk_count, smb2_command_cb cb, void *cb_data)
 {
         struct smb2_ioctl_cb_data *ioctl_data;
         struct smb2_ioctl_request req;
@@ -2871,10 +2649,8 @@ smb2_copychunk_submit_async(struct smb2_context *smb2,
                 smb2_set_error(smb2, "Destination file handle was NULL");
                 return -EINVAL;
         }
-        if (ctl_code != SMB2_FSCTL_SRV_COPYCHUNK &&
-            ctl_code != SMB2_FSCTL_SRV_COPYCHUNK_WRITE) {
-                smb2_set_error(smb2, "Invalid copychunk ctl_code: 0x%08x",
-                               ctl_code);
+        if (ctl_code != SMB2_FSCTL_SRV_COPYCHUNK && ctl_code != SMB2_FSCTL_SRV_COPYCHUNK_WRITE) {
+                smb2_set_error(smb2, "Invalid copychunk ctl_code: 0x%08x", ctl_code);
                 return -EINVAL;
         }
 
@@ -2884,8 +2660,7 @@ smb2_copychunk_submit_async(struct smb2_context *smb2,
                 return -ENOMEM;
         }
 
-        ret = smb2_encode_copychunk_input(smb2, resume_key, chunks,
-                                          chunk_count, &ioctl_data->input,
+        ret = smb2_encode_copychunk_input(smb2, resume_key, chunks, chunk_count, &ioctl_data->input,
                                           &input_count);
         if (ret != 0) {
                 free(ioctl_data);
@@ -2903,8 +2678,7 @@ smb2_copychunk_submit_async(struct smb2_context *smb2,
         req.input = ioctl_data->input;
         req.flags = SMB2_0_IOCTL_IS_FSCTL;
 
-        pdu = smb2_cmd_ioctl_async(smb2, &req, smb2_ioctl_output_cb,
-                                   ioctl_data);
+        pdu = smb2_cmd_ioctl_async(smb2, &req, smb2_ioctl_output_cb, ioctl_data);
         if (pdu == NULL) {
                 free(ioctl_data->input);
                 free(ioctl_data);
@@ -2915,9 +2689,8 @@ smb2_copychunk_submit_async(struct smb2_context *smb2,
         return 0;
 }
 
-int
-smb2_request_resume_key_async(struct smb2_context *smb2, struct smb2fh *fh,
-                              smb2_command_cb cb, void *cb_data)
+int smb2_request_resume_key_async(struct smb2_context *smb2, struct smb2fh *fh, smb2_command_cb cb,
+                                  void *cb_data)
 {
         struct smb2_ioctl_cb_data *ioctl_data;
         struct smb2_ioctl_request req;
@@ -2945,8 +2718,7 @@ smb2_request_resume_key_async(struct smb2_context *smb2, struct smb2fh *fh,
         memcpy(req.file_id, fh->file_id, SMB2_FD_SIZE);
         req.flags = SMB2_0_IOCTL_IS_FSCTL;
 
-        pdu = smb2_cmd_ioctl_async(smb2, &req, smb2_ioctl_output_cb,
-                                   ioctl_data);
+        pdu = smb2_cmd_ioctl_async(smb2, &req, smb2_ioctl_output_cb, ioctl_data);
         if (pdu == NULL) {
                 free(ioctl_data);
                 return -EINVAL;
@@ -2956,17 +2728,13 @@ smb2_request_resume_key_async(struct smb2_context *smb2, struct smb2fh *fh,
         return 0;
 }
 
-int
-smb2_copychunk_async(struct smb2_context *smb2,
-                     uint32_t ctl_code,
-                     const struct smb2_srv_copychunk_resume_key *resume_key,
-                     struct smb2fh *dstfh,
-                     const struct smb2_srv_copychunk *chunks,
-                     uint32_t chunk_count,
-                     smb2_command_cb cb, void *cb_data)
+int smb2_copychunk_async(struct smb2_context *smb2, uint32_t ctl_code,
+                         const struct smb2_srv_copychunk_resume_key *resume_key,
+                         struct smb2fh *dstfh, const struct smb2_srv_copychunk *chunks,
+                         uint32_t chunk_count, smb2_command_cb cb, void *cb_data)
 {
-        return smb2_copychunk_submit_async(smb2, ctl_code, resume_key, dstfh,
-                                           chunks, chunk_count, cb, cb_data);
+        return smb2_copychunk_submit_async(smb2, ctl_code, resume_key, dstfh, chunks, chunk_count,
+                                           cb, cb_data);
 }
 
 struct smb2_server_side_copy_data {
@@ -2978,9 +2746,8 @@ struct smb2_server_side_copy_data {
         struct smb2_srv_copychunk *chunks;
 };
 
-static void
-smb2_server_side_copy_resume_key_cb(struct smb2_context *smb2, int status,
-                                    void *command_data, void *private_data)
+static void smb2_server_side_copy_resume_key_cb(struct smb2_context *smb2, int status,
+                                                void *command_data, void *private_data)
 {
         struct smb2_server_side_copy_data *copy_data = private_data;
         int ret;
@@ -2992,11 +2759,8 @@ smb2_server_side_copy_resume_key_cb(struct smb2_context *smb2, int status,
                 return;
         }
 
-        ret = smb2_copychunk_submit_async(smb2, copy_data->ctl_code,
-                                          command_data, copy_data->dstfh,
-                                          copy_data->chunks,
-                                          copy_data->chunk_count,
-                                          copy_data->cb,
+        ret = smb2_copychunk_submit_async(smb2, copy_data->ctl_code, command_data, copy_data->dstfh,
+                                          copy_data->chunks, copy_data->chunk_count, copy_data->cb,
                                           copy_data->cb_data);
         smb2_free_data(smb2, command_data);
         if (ret != 0) {
@@ -3006,13 +2770,9 @@ smb2_server_side_copy_resume_key_cb(struct smb2_context *smb2, int status,
         free(copy_data);
 }
 
-int
-smb2_server_side_copy_async(struct smb2_context *smb2,
-                            uint32_t ctl_code,
-                            struct smb2fh *srcfh, struct smb2fh *dstfh,
-                            const struct smb2_srv_copychunk *chunks,
-                            uint32_t chunk_count,
-                            smb2_command_cb cb, void *cb_data)
+int smb2_server_side_copy_async(struct smb2_context *smb2, uint32_t ctl_code, struct smb2fh *srcfh,
+                                struct smb2fh *dstfh, const struct smb2_srv_copychunk *chunks,
+                                uint32_t chunk_count, smb2_command_cb cb, void *cb_data)
 {
         struct smb2_server_side_copy_data *copy_data;
         int ret;
@@ -3032,10 +2792,8 @@ smb2_server_side_copy_async(struct smb2_context *smb2,
                 smb2_set_error(smb2, "Chunk array was NULL");
                 return -EINVAL;
         }
-        if (ctl_code != SMB2_FSCTL_SRV_COPYCHUNK &&
-            ctl_code != SMB2_FSCTL_SRV_COPYCHUNK_WRITE) {
-                smb2_set_error(smb2, "Invalid copychunk ctl_code: 0x%08x",
-                               ctl_code);
+        if (ctl_code != SMB2_FSCTL_SRV_COPYCHUNK && ctl_code != SMB2_FSCTL_SRV_COPYCHUNK_WRITE) {
+                smb2_set_error(smb2, "Invalid copychunk ctl_code: 0x%08x", ctl_code);
                 return -EINVAL;
         }
 
@@ -3045,24 +2803,21 @@ smb2_server_side_copy_async(struct smb2_context *smb2,
                 return -ENOMEM;
         }
 
-        copy_data->chunks = calloc(chunk_count,
-                                   sizeof(struct smb2_srv_copychunk));
+        copy_data->chunks = calloc(chunk_count, sizeof(struct smb2_srv_copychunk));
         if (copy_data->chunks == NULL) {
                 free(copy_data);
                 smb2_set_error(smb2, "Failed to allocate chunk copy");
                 return -ENOMEM;
         }
 
-        memcpy(copy_data->chunks, chunks,
-               chunk_count * sizeof(struct smb2_srv_copychunk));
+        memcpy(copy_data->chunks, chunks, chunk_count * sizeof(struct smb2_srv_copychunk));
         copy_data->cb = cb;
         copy_data->cb_data = cb_data;
         copy_data->dstfh = dstfh;
         copy_data->ctl_code = ctl_code;
         copy_data->chunk_count = chunk_count;
 
-        ret = smb2_request_resume_key_async(smb2, srcfh,
-                                            smb2_server_side_copy_resume_key_cb,
+        ret = smb2_request_resume_key_async(smb2, srcfh, smb2_server_side_copy_resume_key_cb,
                                             copy_data);
         if (ret != 0) {
                 free(copy_data->chunks);
@@ -3077,9 +2832,8 @@ struct disconnect_data {
         void *cb_data;
 };
 
-static void
-disconnect_cb_2(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void disconnect_cb_2(struct smb2_context *smb2, int status, void *command_data _U_,
+                            void *private_data)
 {
         struct disconnect_data *dc_data = private_data;
 
@@ -3092,9 +2846,8 @@ disconnect_cb_2(struct smb2_context *smb2, int status,
         smb2->fd = SMB2_INVALID_SOCKET;
 }
 
-static void
-disconnect_cb_1(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void disconnect_cb_1(struct smb2_context *smb2, int status, void *command_data _U_,
+                            void *private_data)
 {
         struct disconnect_data *dc_data = private_data;
         struct smb2_pdu *pdu;
@@ -3114,9 +2867,7 @@ disconnect_cb_1(struct smb2_context *smb2, int status,
         smb2_queue_pdu(smb2, pdu);
 }
 
-int
-smb2_disconnect_share_async(struct smb2_context *smb2,
-                            smb2_command_cb cb, void *cb_data)
+int smb2_disconnect_share_async(struct smb2_context *smb2, smb2_command_cb cb, void *cb_data)
 {
         struct disconnect_data *dc_data;
         struct smb2_pdu *pdu;
@@ -3154,20 +2905,16 @@ struct smb2_echo_data {
         void *cb_data;
 };
 
-static void
-echo_cb(struct smb2_context *smb2, int status,
-           void *command_data _U_, void *private_data)
+static void echo_cb(struct smb2_context *smb2, int status, void *command_data _U_,
+                    void *private_data)
 {
         struct smb2_echo_data *cb_data = private_data;
 
-        cb_data->cb(smb2, -nterror_to_errno(status),
-                    NULL, cb_data->cb_data);
+        cb_data->cb(smb2, -nterror_to_errno(status), NULL, cb_data->cb_data);
         free(cb_data);
 }
 
-int
-smb2_echo_async(struct smb2_context *smb2,
-                smb2_command_cb cb, void *cb_data)
+int smb2_echo_async(struct smb2_context *smb2, smb2_command_cb cb, void *cb_data)
 {
         struct smb2_echo_data *echo_data;
         struct smb2_pdu *pdu;
@@ -3195,26 +2942,22 @@ smb2_echo_async(struct smb2_context *smb2,
         return 0;
 }
 
-uint32_t
-smb2_get_max_read_size(struct smb2_context *smb2)
+uint32_t smb2_get_max_read_size(struct smb2_context *smb2)
 {
         return smb2->max_read_size;
 }
 
-uint32_t
-smb2_get_max_write_size(struct smb2_context *smb2)
+uint32_t smb2_get_max_write_size(struct smb2_context *smb2)
 {
         return smb2->max_write_size;
 }
 
-smb2_file_id *
-smb2_get_file_id(struct smb2fh *fh)
+smb2_file_id *smb2_get_file_id(struct smb2fh *fh)
 {
         return &fh->file_id;
 }
 
-struct smb2fh *
-smb2_fh_from_file_id(struct smb2_context *smb2, smb2_file_id *fileid)
+struct smb2fh *smb2_fh_from_file_id(struct smb2_context *smb2, smb2_file_id *fileid)
 {
         struct smb2fh *fh;
 
@@ -3227,17 +2970,15 @@ smb2_fh_from_file_id(struct smb2_context *smb2, smb2_file_id *fileid)
         return fh;
 }
 
-void
-smb2_fd_event_callbacks(struct smb2_context *smb2,
-                        smb2_change_fd_cb change_fd,
-                        smb2_change_events_cb change_events)
+void smb2_fd_event_callbacks(struct smb2_context *smb2, smb2_change_fd_cb change_fd,
+                             smb2_change_events_cb change_events)
 {
         smb2->change_fd = change_fd;
         smb2->change_events = change_events;
 }
 
-void
-smb2_oplock_break_notify(struct smb2_context *smb2, int status, void *command_data, void *cb_data)
+void smb2_oplock_break_notify(struct smb2_context *smb2, int status, void *command_data,
+                              void *cb_data)
 {
         struct smb2_oplock_or_lease_break_reply *rep;
         struct smb2_oplock_break_reply rep_oplock;
@@ -3246,24 +2987,25 @@ smb2_oplock_break_notify(struct smb2_context *smb2, int status, void *command_da
         uint8_t new_oplock_level;
         uint32_t new_lease_state;
 
-        rep= command_data;
-
+        rep = command_data;
 
         if (smb2->oplock_or_lease_break_cb) {
-                smb2->oplock_or_lease_break_cb(smb2,
-                               status, rep, &new_oplock_level, &new_lease_state);
+                smb2->oplock_or_lease_break_cb(smb2, status, rep, &new_oplock_level,
+                                               &new_lease_state);
         }
         /* for passthrough case assume the app callback will do everything needed
          */
         if (!smb2->passthrough) {
                 if (status) {
                         return;
-                } else switch (rep->break_type) {
+                } else
+                        switch (rep->break_type) {
                         case SMB2_BREAK_TYPE_OPLOCK_NOTIFICATION:
                                 memset(&rep_oplock, 0, sizeof(rep_oplock));
                                 rep_oplock.oplock_level = new_oplock_level;
                                 memcpy(rep_oplock.file_id, rep->lock.oplock.file_id, SMB2_FD_SIZE);
-                                pdu = smb2_cmd_oplock_break_reply_async(smb2, &rep_oplock, NULL, cb_data);
+                                pdu = smb2_cmd_oplock_break_reply_async(smb2, &rep_oplock, NULL,
+                                                                        cb_data);
                                 break;
                         case SMB2_BREAK_TYPE_OPLOCK_RESPONSE:
                                 break;
@@ -3271,50 +3013,51 @@ smb2_oplock_break_notify(struct smb2_context *smb2, int status, void *command_da
                                 memset(&rep_lease, 0, sizeof(rep_oplock));
                                 rep_lease.flags = rep->lock.lease.flags;
                                 rep_lease.lease_state = new_lease_state;
-                                memcpy(rep_lease.lease_key, rep->lock.lease.lease_key, SMB2_LEASE_KEY_SIZE);
-                                pdu = smb2_cmd_lease_break_reply_async(smb2, &rep_lease, NULL, cb_data);
+                                memcpy(rep_lease.lease_key, rep->lock.lease.lease_key,
+                                       SMB2_LEASE_KEY_SIZE);
+                                pdu = smb2_cmd_lease_break_reply_async(smb2, &rep_lease, NULL,
+                                                                       cb_data);
                                 break;
                         case SMB2_BREAK_TYPE_LEASE_RESPONSE:
                                 break;
                         default:
                                 smb2_set_error(smb2, "Bad oplock/lease break request %s",
-                                                smb2_get_error(smb2));
+                                               smb2_get_error(smb2));
                                 return;
-                }
+                        }
                 if (pdu != NULL) {
                         smb2_queue_pdu(smb2, pdu);
                 }
         }
 }
 
-int
-smb2_decode_filenotifychangeinformation(
-    struct smb2_context *smb2,
-    struct smb2_file_notify_change_information *fnc,
-    struct smb2_iovec *vec,
-    uint32_t next_entry_offset)
+int smb2_decode_filenotifychangeinformation(struct smb2_context *smb2,
+                                            struct smb2_file_notify_change_information *fnc,
+                                            struct smb2_iovec *vec, uint32_t next_entry_offset)
 {
         uint32_t name_len, tmp;
 
         if (next_entry_offset + 12 > vec->len) {
                 return 0;
         }
-        smb2_get_uint32(vec, next_entry_offset+4, &fnc->action);
-        smb2_get_uint32(vec, next_entry_offset+8, &name_len);
-        fnc->name = smb2_utf16_to_utf8((uint16_t *)(void *)&vec->buf[next_entry_offset+12], name_len / 2);
+        smb2_get_uint32(vec, next_entry_offset + 4, &fnc->action);
+        smb2_get_uint32(vec, next_entry_offset + 8, &name_len);
+        fnc->name =
+            smb2_utf16_to_utf8((uint16_t *)(void *)&vec->buf[next_entry_offset + 12], name_len / 2);
 
         smb2_get_uint32(vec, next_entry_offset, &tmp);
         next_entry_offset += tmp;
         if (tmp != 0) {
-                struct smb2_file_notify_change_information *next_fnc = calloc(1, sizeof(struct smb2_file_notify_change_information));
+                struct smb2_file_notify_change_information *next_fnc =
+                    calloc(1, sizeof(struct smb2_file_notify_change_information));
                 fnc->next = next_fnc;
                 smb2_decode_filenotifychangeinformation(smb2, next_fnc, vec, next_entry_offset);
         }
         return 0;
 }
 
-void
-free_smb2_file_notify_change_information(struct smb2_context *smb2, struct smb2_file_notify_change_information *fnc)
+void free_smb2_file_notify_change_information(struct smb2_context *smb2,
+                                              struct smb2_file_notify_change_information *fnc)
 {
         if (fnc->next) {
                 free_smb2_file_notify_change_information(smb2, fnc->next);
@@ -3337,19 +3080,19 @@ struct notify_change_cb_data {
         uint32_t status;
 };
 
-static void
-notify_change_cb(struct smb2_context *smb2, int status,
-          void *command_data _U_, void *private_data)
+static void notify_change_cb(struct smb2_context *smb2, int status, void *command_data _U_,
+                             void *private_data)
 {
         struct notify_change_cb_data *notify_change_data = private_data;
 
         struct smb2_change_notify_reply *rep = command_data;
         struct smb2_iovec vec;
-        struct smb2_file_notify_change_information *fnc = calloc(1, sizeof(struct smb2_file_notify_change_information));
+        struct smb2_file_notify_change_information *fnc =
+            calloc(1, sizeof(struct smb2_file_notify_change_information));
 
         if (status) {
-                smb2_set_error(smb2, "notify_change_cb failed (%s) %s\n",
-                               strerror(-status), smb2_get_error(smb2));
+                smb2_set_error(smb2, "notify_change_cb failed (%s) %s\n", strerror(-status),
+                               smb2_get_error(smb2));
         }
 
         if (status || rep == NULL) {
@@ -3359,8 +3102,8 @@ notify_change_cb(struct smb2_context *smb2, int status,
                  * the unconditional rep->output dereference below SEGVs whenever a watcher
                  * is torn down with a notify request still outstanding. */
                 if (notify_change_data->cb) {
-                        notify_change_data->cb(smb2, status ? status : -EIO,
-                                               NULL, notify_change_data->cb_data);
+                        notify_change_data->cb(smb2, status ? status : -EIO, NULL,
+                                               notify_change_data->cb_data);
                 }
                 free(fnc);
                 free(notify_change_data);
@@ -3375,24 +3118,23 @@ notify_change_cb(struct smb2_context *smb2, int status,
         }
 
         if (notify_change_data->cb) {
-                notify_change_data->cb(
-                        smb2,
-                        -nterror_to_errno(notify_change_data->status),
-                        fnc,
-                        notify_change_data->cb_data
-                );
+                notify_change_data->cb(smb2, -nterror_to_errno(notify_change_data->status), fnc,
+                                       notify_change_data->cb_data);
         }
         if (notify_change_data->loop) {
-                smb2_notify_change_filehandle_async(smb2, notify_change_data->fh, notify_change_data->flags, notify_change_data->filter,
-                        notify_change_data->loop, notify_change_data->cb, notify_change_data->cb_data);
+                smb2_notify_change_filehandle_async(
+                    smb2, notify_change_data->fh, notify_change_data->flags,
+                    notify_change_data->filter, notify_change_data->loop, notify_change_data->cb,
+                    notify_change_data->cb_data);
         } else {
                 smb2_close(smb2, notify_change_data->fh);
         }
         free(notify_change_data);
 }
 
-int smb2_notify_change_filehandle_async(struct smb2_context *smb2, struct smb2fh *smb2_dir_fh, uint16_t flags, uint32_t filter, int loop,
-                       smb2_command_cb cb, void *cb_data)
+int smb2_notify_change_filehandle_async(struct smb2_context *smb2, struct smb2fh *smb2_dir_fh,
+                                        uint16_t flags, uint32_t filter, int loop,
+                                        smb2_command_cb cb, void *cb_data)
 {
         struct notify_change_cb_data *notify_change_cb_data;
         struct smb2_change_notify_request ch_req;
@@ -3420,8 +3162,7 @@ int smb2_notify_change_filehandle_async(struct smb2_context *smb2, struct smb2fh
         memcpy(ch_req.file_id, file_id, SMB2_FD_SIZE);
         ch_req.completion_filter = filter;
 
-        pdu = smb2_cmd_change_notify_async(smb2, &ch_req,
-                                             notify_change_cb, notify_change_cb_data);
+        pdu = smb2_cmd_change_notify_async(smb2, &ch_req, notify_change_cb, notify_change_cb_data);
         if (pdu == NULL) {
                 smb2_set_error(smb2, "Failed to create change_notify command\n");
                 free(notify_change_cb_data);
@@ -3432,8 +3173,8 @@ int smb2_notify_change_filehandle_async(struct smb2_context *smb2, struct smb2fh
         return 0;
 }
 
-int smb2_notify_change_async(struct smb2_context *smb2, const char *path, uint16_t flags, uint32_t filter, int loop,
-                       smb2_command_cb cb, void *cb_data)
+int smb2_notify_change_async(struct smb2_context *smb2, const char *path, uint16_t flags,
+                             uint32_t filter, int loop, smb2_command_cb cb, void *cb_data)
 {
         struct smb2fh *fh;
 #ifdef O_DIRECTORY
@@ -3446,12 +3187,11 @@ int smb2_notify_change_async(struct smb2_context *smb2, const char *path, uint16
                 return -1;
         }
         return smb2_notify_change_filehandle_async(smb2, fh, flags, filter, loop, cb, cb_data);
-
 }
 
-/*************************** server handlers *************************************************************/
-static uint32_t
-smb2_server_status_from_errno(enum smb2_command cmd, int ret)
+/*************************** server handlers
+ * *************************************************************/
+static uint32_t smb2_server_status_from_errno(enum smb2_command cmd, int ret)
 {
         int err;
 
@@ -3496,50 +3236,81 @@ smb2_server_status_from_errno(enum smb2_command cmd, int ret)
 /* Server-side error log. Surfaces handler errnos and stalled (NULL)
  * reply PDUs so they are visible in the iOS/macOS Console without a
  * packet capture. Kept short, one line, low volume. */
-static const char *
-smb2_server_cmd_name(enum smb2_command cmd)
+static const char *smb2_server_cmd_name(enum smb2_command cmd)
 {
         switch (cmd) {
-        case SMB2_NEGOTIATE:       return "NEGOTIATE";
-        case SMB2_SESSION_SETUP:   return "SESSION_SETUP";
-        case SMB2_LOGOFF:          return "LOGOFF";
-        case SMB2_TREE_CONNECT:    return "TREE_CONNECT";
-        case SMB2_TREE_DISCONNECT: return "TREE_DISCONNECT";
-        case SMB2_CREATE:          return "CREATE";
-        case SMB2_CLOSE:           return "CLOSE";
-        case SMB2_FLUSH:           return "FLUSH";
-        case SMB2_READ:            return "READ";
-        case SMB2_WRITE:           return "WRITE";
-        case SMB2_LOCK:            return "LOCK";
-        case SMB2_IOCTL:           return "IOCTL";
-        case SMB2_CANCEL:          return "CANCEL";
-        case SMB2_ECHO:            return "ECHO";
-        case SMB2_QUERY_DIRECTORY: return "QUERY_DIRECTORY";
-        case SMB2_CHANGE_NOTIFY:   return "CHANGE_NOTIFY";
-        case SMB2_QUERY_INFO:      return "QUERY_INFO";
-        case SMB2_SET_INFO:        return "SET_INFO";
-        case SMB2_OPLOCK_BREAK:    return "OPLOCK_BREAK";
-        default:                   return "UNKNOWN";
+        case SMB2_NEGOTIATE:
+                return "NEGOTIATE";
+        case SMB2_SESSION_SETUP:
+                return "SESSION_SETUP";
+        case SMB2_LOGOFF:
+                return "LOGOFF";
+        case SMB2_TREE_CONNECT:
+                return "TREE_CONNECT";
+        case SMB2_TREE_DISCONNECT:
+                return "TREE_DISCONNECT";
+        case SMB2_CREATE:
+                return "CREATE";
+        case SMB2_CLOSE:
+                return "CLOSE";
+        case SMB2_FLUSH:
+                return "FLUSH";
+        case SMB2_READ:
+                return "READ";
+        case SMB2_WRITE:
+                return "WRITE";
+        case SMB2_LOCK:
+                return "LOCK";
+        case SMB2_IOCTL:
+                return "IOCTL";
+        case SMB2_CANCEL:
+                return "CANCEL";
+        case SMB2_ECHO:
+                return "ECHO";
+        case SMB2_QUERY_DIRECTORY:
+                return "QUERY_DIRECTORY";
+        case SMB2_CHANGE_NOTIFY:
+                return "CHANGE_NOTIFY";
+        case SMB2_QUERY_INFO:
+                return "QUERY_INFO";
+        case SMB2_SET_INFO:
+                return "SET_INFO";
+        case SMB2_OPLOCK_BREAK:
+                return "OPLOCK_BREAK";
+        default:
+                return "UNKNOWN";
         }
 }
 
 #ifdef __APPLE__
 #include <os/log.h>
-static void
-smb2_server_log_line(const char *line)
+static void smb2_server_log_line(const char *line)
 {
+#ifdef INAS_DEFER_DEBUG
+        /* Debug builds mirror server errors into the app's Documents dir
+         * so they can be pulled off the device (os_log debug is not
+         * visible through devicectl). */
+        const char *home = getenv("HOME");
+        if (home) {
+                char path[512];
+                snprintf(path, sizeof(path), "%s/Documents/smb-errors.log", home);
+                FILE *df = fopen(path, "a");
+                if (df) {
+                        fprintf(df, "%s\n", line);
+                        fclose(df);
+                }
+        }
+#endif
         os_log_debug(OS_LOG_DEFAULT, "inas-smb: %{public}s", line);
 }
 #else
-static void
-smb2_server_log_line(const char *line)
+static void smb2_server_log_line(const char *line)
 {
         fprintf(stderr, "inas-smb: %s\n", line);
 }
 #endif
 
-static void
-smb2_server_log_err(enum smb2_command cmd, int ret, struct smb2_pdu *pdu)
+static void smb2_server_log_err(enum smb2_command cmd, int ret, struct smb2_pdu *pdu)
 {
         char line[128];
 
@@ -3555,18 +3326,16 @@ smb2_server_log_err(enum smb2_command cmd, int ret, struct smb2_pdu *pdu)
 
 /* Always-on one-line trace for commands implicated in client teardowns
  * (SET_INFO / CHANGE_NOTIFY). Trailing spaces trimmed by the reader. */
-static void
-smb2_server_log_trace(enum smb2_command cmd, int a, int b)
+static void smb2_server_log_trace(enum smb2_command cmd, int a, int b)
 {
         char line[128];
 
-        snprintf(line, sizeof(line), "trace cmd=%s a=%d b=%d",
-                 smb2_server_cmd_name(cmd), a, b);
+        snprintf(line, sizeof(line), "trace cmd=%s a=%d b=%d", smb2_server_cmd_name(cmd), a, b);
         smb2_server_log_line(line);
 }
 
-static void
-smb2_logoff_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_logoff_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                   void *command_data, void *cb_data)
 {
         struct smb2_pdu *pdu = NULL;
         struct smb2_error_reply err;
@@ -3577,11 +3346,11 @@ smb2_logoff_request_cb(struct smb2_server *server, struct smb2_context *smb2, vo
         }
         if (!ret) {
                 pdu = smb2_cmd_logoff_reply_async(smb2, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_LOGOFF, smb2_server_status_from_errno(SMB2_LOGOFF, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_LOGOFF,
+                                                 smb2_server_status_from_errno(SMB2_LOGOFF, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3589,8 +3358,8 @@ smb2_logoff_request_cb(struct smb2_server *server, struct smb2_context *smb2, vo
         }
 }
 
-static void
-smb2_tree_connect_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_tree_connect_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                         void *command_data, void *cb_data)
 {
         struct smb2_tree_connect_request *req = command_data;
         struct smb2_tree_connect_reply rep;
@@ -3604,11 +3373,11 @@ smb2_tree_connect_request_cb(struct smb2_server *server, struct smb2_context *sm
         }
         if (!ret) {
                 pdu = smb2_cmd_tree_connect_reply_async(smb2, &rep, rep.tree_id, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_TREE_CONNECT, smb2_server_status_from_errno(SMB2_TREE_CONNECT, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(
+                    smb2, &err, SMB2_TREE_CONNECT,
+                    smb2_server_status_from_errno(SMB2_TREE_CONNECT, ret), NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3616,8 +3385,8 @@ smb2_tree_connect_request_cb(struct smb2_server *server, struct smb2_context *sm
         }
 }
 
-static void
-smb2_tree_disconnect_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_tree_disconnect_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                            void *command_data, void *cb_data)
 {
         struct smb2_pdu *pdu = NULL;
         struct smb2_error_reply err;
@@ -3629,11 +3398,11 @@ smb2_tree_disconnect_request_cb(struct smb2_server *server, struct smb2_context
         }
         if (!ret) {
                 pdu = smb2_cmd_tree_disconnect_reply_async(smb2, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_TREE_DISCONNECT, smb2_server_status_from_errno(SMB2_TREE_DISCONNECT, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(
+                    smb2, &err, SMB2_TREE_DISCONNECT,
+                    smb2_server_status_from_errno(SMB2_TREE_DISCONNECT, ret), NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3643,8 +3412,8 @@ smb2_tree_disconnect_request_cb(struct smb2_server *server, struct smb2_context
         smb2_disconnect_tree_id(smb2, tree_id);
 }
 
-static void
-smb2_create_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_create_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                   void *command_data, void *cb_data)
 {
         struct smb2_create_request *req = command_data;
         struct smb2_create_reply rep;
@@ -3665,14 +3434,13 @@ smb2_create_request_cb(struct smb2_server *server, struct smb2_context *smb2, vo
                 if (pdu == NULL) {
                         memset(&err, 0, sizeof(err));
                         pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_CREATE,
-                                                         SMB2_STATUS_UNSUCCESSFUL,
-                                                         NULL, cb_data);
+                                                         SMB2_STATUS_UNSUCCESSFUL, NULL, cb_data);
                 }
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_CREATE, smb2_server_status_from_errno(SMB2_CREATE, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_CREATE,
+                                                 smb2_server_status_from_errno(SMB2_CREATE, ret),
+                                                 NULL, cb_data);
         }
         if (pdu) {
                 if (req->name) {
@@ -3684,8 +3452,8 @@ smb2_create_request_cb(struct smb2_server *server, struct smb2_context *smb2, vo
         smb2_server_log_err(SMB2_CREATE, ret, pdu);
 }
 
-static void
-smb2_close_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_close_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                  void *command_data, void *cb_data)
 {
         struct smb2_close_request *req = command_data;
         struct smb2_close_reply rep;
@@ -3699,11 +3467,11 @@ smb2_close_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
         }
         if (!ret) {
                 pdu = smb2_cmd_close_reply_async(smb2, &rep, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_CLOSE, smb2_server_status_from_errno(SMB2_CLOSE, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_CLOSE,
+                                                 smb2_server_status_from_errno(SMB2_CLOSE, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3711,8 +3479,8 @@ smb2_close_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
         }
 }
 
-static void
-smb2_flush_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_flush_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                  void *command_data, void *cb_data)
 {
         struct smb2_flush_request *req = command_data;
         struct smb2_error_reply err;
@@ -3724,11 +3492,11 @@ smb2_flush_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
         }
         if (!ret) {
                 pdu = smb2_cmd_flush_reply_async(smb2, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_FLUSH, smb2_server_status_from_errno(SMB2_FLUSH, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_FLUSH,
+                                                 smb2_server_status_from_errno(SMB2_FLUSH, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3736,8 +3504,8 @@ smb2_flush_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
         }
 }
 
-static void
-smb2_read_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_read_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                 void *command_data, void *cb_data)
 {
         struct smb2_read_request *req = command_data;
         struct smb2_read_reply rep;
@@ -3751,11 +3519,11 @@ smb2_read_request_cb(struct smb2_server *server, struct smb2_context *smb2, void
         }
         if (!ret) {
                 pdu = smb2_cmd_read_reply_async(smb2, &rep, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_READ, smb2_server_status_from_errno(SMB2_READ, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_READ,
+                                                 smb2_server_status_from_errno(SMB2_READ, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3763,8 +3531,8 @@ smb2_read_request_cb(struct smb2_server *server, struct smb2_context *smb2, void
         }
 }
 
-static void
-smb2_write_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_write_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                  void *command_data, void *cb_data)
 {
         struct smb2_write_request *req = command_data;
         struct smb2_write_reply rep;
@@ -3778,11 +3546,11 @@ smb2_write_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
         }
         if (!ret) {
                 pdu = smb2_cmd_write_reply_async(smb2, &rep, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_WRITE, smb2_server_status_from_errno(SMB2_WRITE, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_WRITE,
+                                                 smb2_server_status_from_errno(SMB2_WRITE, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3790,8 +3558,8 @@ smb2_write_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
         }
 }
 
-static void
-smb2_oplock_break_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_oplock_break_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                         void *command_data, void *cb_data)
 {
         struct smb2_oplock_or_lease_break_request *req = command_data;
         struct smb2_oplock_break_reply rep_oplock;
@@ -3802,31 +3570,29 @@ smb2_oplock_break_request_cb(struct smb2_server *server, struct smb2_context *sm
 
         if (req->struct_size == SMB2_OPLOCK_BREAK_NOTIFICATION_SIZE) {
                 if (server->handlers && server->handlers->oplock_break_cmd) {
-                        ret = server->handlers->oplock_break_cmd(server, smb2,
-                                       &req->lock.oplock);
+                        ret = server->handlers->oplock_break_cmd(server, smb2, &req->lock.oplock);
                         if (!ret) {
                                 memset(&rep_oplock, 0, sizeof(rep_oplock));
-                                pdu = smb2_cmd_oplock_break_reply_async(smb2,
-                                        &rep_oplock, NULL, cb_data);
+                                pdu = smb2_cmd_oplock_break_reply_async(smb2, &rep_oplock, NULL,
+                                                                        cb_data);
                         }
                 }
-        }
-        else if ((req->struct_size == SMB2_LEASE_BREAK_NOTIFICATION_SIZE) |
-                        (req->struct_size == SMB2_LEASE_BREAK_REPLY_SIZE)) {
+        } else if ((req->struct_size == SMB2_LEASE_BREAK_NOTIFICATION_SIZE) |
+                   (req->struct_size == SMB2_LEASE_BREAK_REPLY_SIZE)) {
                 if (server->handlers && server->handlers->lease_break_cmd) {
-                        ret = server->handlers->lease_break_cmd(server, smb2,
-                                       &req->lock.lease);
+                        ret = server->handlers->lease_break_cmd(server, smb2, &req->lock.lease);
                         if (!ret) {
                                 memset(&rep_lease, 0, sizeof(rep_lease));
-                                pdu = smb2_cmd_lease_break_reply_async(smb2,
-                                        &rep_lease, NULL, cb_data);
+                                pdu = smb2_cmd_lease_break_reply_async(smb2, &rep_lease, NULL,
+                                                                       cb_data);
                         }
                 }
         }
-        if(ret < 0) {
+        if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_LOCK, smb2_server_status_from_errno(SMB2_LOCK, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_LOCK,
+                                                 smb2_server_status_from_errno(SMB2_LOCK, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3834,8 +3600,8 @@ smb2_oplock_break_request_cb(struct smb2_server *server, struct smb2_context *sm
         }
 }
 
-static void
-smb2_lock_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_lock_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                 void *command_data, void *cb_data)
 {
         struct smb2_lock_request *req = command_data;
         struct smb2_error_reply err;
@@ -3847,11 +3613,11 @@ smb2_lock_request_cb(struct smb2_server *server, struct smb2_context *smb2, void
         }
         if (!ret) {
                 pdu = smb2_cmd_lock_reply_async(smb2, NULL, cb_data);
-        }
-        else if(ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_LOCK, smb2_server_status_from_errno(SMB2_LOCK, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_LOCK,
+                                                 smb2_server_status_from_errno(SMB2_LOCK, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3859,8 +3625,8 @@ smb2_lock_request_cb(struct smb2_server *server, struct smb2_context *smb2, void
         }
 }
 
-static void
-smb2_ioctl_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_ioctl_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                  void *command_data, void *cb_data)
 {
         struct smb2_ioctl_request *req = command_data;
         struct smb2_ioctl_reply rep;
@@ -3880,24 +3646,23 @@ smb2_ioctl_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
                 out_info.security_mode = smb2->security_mode;
                 memcpy(out_info.guid, server->guid, 16);
                 out_info.dialect = smb2->dialect;
-                rep.output = (uint8_t*)&out_info;
+                rep.output = (uint8_t *)&out_info;
                 rep.output_count = sizeof(out_info);
                 pdu = smb2_cmd_ioctl_reply_async(smb2, &rep, NULL, cb_data);
                 if (req->input) {
                         smb2_free_data(smb2, discard_const(req->input));
                 }
-        }
-        else {
+        } else {
                 if (server->handlers && server->handlers->ioctl_cmd) {
                         ret = server->handlers->ioctl_cmd(server, smb2, req, &rep);
                 }
                 if (!ret) {
                         pdu = smb2_cmd_ioctl_reply_async(smb2, &rep, NULL, cb_data);
-                }
-                else if (ret < 0) {
+                } else if (ret < 0) {
                         memset(&err, 0, sizeof(err));
-                        pdu = smb2_cmd_error_reply_async(smb2,
-                                        &err, SMB2_IOCTL, smb2_server_status_from_errno(SMB2_IOCTL, ret), NULL, cb_data);
+                        pdu = smb2_cmd_error_reply_async(
+                            smb2, &err, SMB2_IOCTL, smb2_server_status_from_errno(SMB2_IOCTL, ret),
+                            NULL, cb_data);
                 }
                 if (req->ctl_code == SMB2_FSCTL_PIPE_TRANSCEIVE && req->input) {
                         smb2_free_data(smb2, discard_const(req->input));
@@ -3910,8 +3675,8 @@ smb2_ioctl_request_cb(struct smb2_server *server, struct smb2_context *smb2, voi
         }
 }
 
-static void
-smb2_cancel_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_cancel_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                   void *command_data, void *cb_data)
 {
         struct smb2_error_reply err;
         struct smb2_pdu *pdu = NULL;
@@ -3922,8 +3687,9 @@ smb2_cancel_request_cb(struct smb2_server *server, struct smb2_context *smb2, vo
         }
         if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_CANCEL, smb2_server_status_from_errno(SMB2_CANCEL, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_CANCEL,
+                                                 smb2_server_status_from_errno(SMB2_CANCEL, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3931,8 +3697,8 @@ smb2_cancel_request_cb(struct smb2_server *server, struct smb2_context *smb2, vo
         }
 }
 
-static void
-smb2_echo_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_echo_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                 void *command_data, void *cb_data)
 {
         struct smb2_error_reply err;
         struct smb2_pdu *pdu = NULL;
@@ -3943,11 +3709,11 @@ smb2_echo_request_cb(struct smb2_server *server, struct smb2_context *smb2, void
         }
         if (!ret) {
                 pdu = smb2_cmd_echo_reply_async(smb2, NULL, cb_data);
-        }
-        else if (ret < 0) {
+        } else if (ret < 0) {
                 memset(&err, 0, sizeof(err));
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_ECHO, smb2_server_status_from_errno(SMB2_ECHO, ret), NULL, cb_data);
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_ECHO,
+                                                 smb2_server_status_from_errno(SMB2_ECHO, ret),
+                                                 NULL, cb_data);
         }
         if (pdu != NULL) {
                 smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
@@ -3955,8 +3721,8 @@ smb2_echo_request_cb(struct smb2_server *server, struct smb2_context *smb2, void
         }
 }
 
-static void
-smb2_query_directory_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_query_directory_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                            void *command_data, void *cb_data)
 {
         struct smb2_query_directory_request *req = command_data;
         struct smb2_query_directory_reply rep;
@@ -3971,24 +3737,28 @@ smb2_query_directory_request_cb(struct smb2_server *server, struct smb2_context
                 ret = server->handlers->query_directory_cmd(server, smb2, req, &rep);
         }
         if (ret < 0) {
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_QUERY_DIRECTORY, smb2_server_status_from_errno(SMB2_QUERY_DIRECTORY, ret), NULL, cb_data);
-        }
-        else if (!ret) {
+                pdu = smb2_cmd_error_reply_async(
+                    smb2, &err, SMB2_QUERY_DIRECTORY,
+                    smb2_server_status_from_errno(SMB2_QUERY_DIRECTORY, ret), NULL, cb_data);
+        } else if (!ret) {
                 if (rep.output_buffer_length == 0) {
-                        pdu = smb2_cmd_error_reply_async(smb2,
-                                        &err, SMB2_QUERY_DIRECTORY, SMB2_STATUS_NO_MORE_FILES, NULL, cb_data);
-                }
-                else if (rep.output_buffer_length < 0) {
-                        pdu = smb2_cmd_error_reply_async(smb2,
-                                        &err, SMB2_QUERY_DIRECTORY, SMB2_STATUS_NOT_SUPPORTED, NULL, cb_data);
-                }
-                else {
+                        pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_QUERY_DIRECTORY,
+                                                         SMB2_STATUS_NO_MORE_FILES, NULL, cb_data);
+                } else if (rep.output_buffer_length >= 0xFFFFFFFEu) {
+                        /* Handler-encoded empty-result statuses: 0xFFFFFFFF
+                         * = enumeration exhausted (NO_MORE_FILES),
+                         * 0xFFFFFFFE = nothing matched (NO_SUCH_FILE). */
+                        uint32_t st = rep.output_buffer_length == 0xFFFFFFFFu
+                                          ? SMB2_STATUS_NO_MORE_FILES
+                                          : SMB2_STATUS_NO_SUCH_FILE;
+                        pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_QUERY_DIRECTORY, st, NULL,
+                                                         cb_data);
+                } else {
                         pdu = smb2_cmd_query_directory_reply_async(smb2, req, &rep, NULL, cb_data);
                         if (pdu == NULL) {
                                 pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_QUERY_DIRECTORY,
-                                                                 SMB2_STATUS_UNSUCCESSFUL,
-                                                                 NULL, cb_data);
+                                                                 SMB2_STATUS_UNSUCCESSFUL, NULL,
+                                                                 cb_data);
                         }
                 }
         }
@@ -4006,8 +3776,8 @@ smb2_query_directory_request_cb(struct smb2_server *server, struct smb2_context
         smb2_server_log_err(SMB2_QUERY_DIRECTORY, ret, pdu);
 }
 
-static void
-smb2_change_notify_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_change_notify_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                          void *command_data, void *cb_data)
 {
         struct smb2_change_notify_request *req = command_data;
         struct smb2_change_notify_reply rep;
@@ -4022,15 +3792,14 @@ smb2_change_notify_request_cb(struct smb2_server *server, struct smb2_context *s
                 ret = server->handlers->change_notify_cmd(server, smb2, req, &rep);
         }
         if (ret < 0) {
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_CHANGE_NOTIFY, smb2_server_status_from_errno(SMB2_CHANGE_NOTIFY, ret), NULL, cb_data);
-        }
-        else if (!ret) {
+                pdu = smb2_cmd_error_reply_async(
+                    smb2, &err, SMB2_CHANGE_NOTIFY,
+                    smb2_server_status_from_errno(SMB2_CHANGE_NOTIFY, ret), NULL, cb_data);
+        } else if (!ret) {
                 pdu = smb2_cmd_change_notify_reply_async(smb2, &rep, NULL, cb_data);
                 if (pdu == NULL) {
                         pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_CHANGE_NOTIFY,
-                                                         SMB2_STATUS_UNSUCCESSFUL,
-                                                         NULL, cb_data);
+                                                         SMB2_STATUS_UNSUCCESSFUL, NULL, cb_data);
                 }
         }
         if (pdu != NULL) {
@@ -4041,8 +3810,8 @@ smb2_change_notify_request_cb(struct smb2_server *server, struct smb2_context *s
         smb2_server_log_err(SMB2_CHANGE_NOTIFY, ret, pdu);
 }
 
-static void
-smb2_query_info_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_query_info_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                       void *command_data, void *cb_data)
 {
         struct smb2_query_info_request *req = command_data;
         struct smb2_query_info_reply rep;
@@ -4057,24 +3826,23 @@ smb2_query_info_request_cb(struct smb2_server *server, struct smb2_context *smb2
                 ret = server->handlers->query_info_cmd(server, smb2, req, &rep);
         }
         if (ret < 0) {
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_QUERY_INFO, smb2_server_status_from_errno(SMB2_QUERY_INFO, ret), NULL, cb_data);
-        }
-        else if (!ret) {
+                pdu = smb2_cmd_error_reply_async(
+                    smb2, &err, SMB2_QUERY_INFO,
+                    smb2_server_status_from_errno(SMB2_QUERY_INFO, ret), NULL, cb_data);
+        } else if (!ret) {
                 if (rep.output_buffer_length == 0) {
-                        pdu = smb2_cmd_error_reply_async(smb2,
-                                        &err, SMB2_QUERY_INFO, SMB2_STATUS_NOT_SUPPORTED, NULL, cb_data);
-                }
-                else if (rep.output_buffer_length < 0) {
-                        pdu = smb2_cmd_error_reply_async(smb2,
-                                        &err, SMB2_QUERY_INFO, SMB2_STATUS_INVALID_INFO_CLASS, NULL, cb_data);
-                }
-                else {
+                        pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_QUERY_INFO,
+                                                         SMB2_STATUS_NOT_SUPPORTED, NULL, cb_data);
+                } else if (rep.output_buffer_length < 0) {
+                        pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_QUERY_INFO,
+                                                         SMB2_STATUS_INVALID_INFO_CLASS, NULL,
+                                                         cb_data);
+                } else {
                         pdu = smb2_cmd_query_info_reply_async(smb2, req, &rep, NULL, cb_data);
                         if (pdu == NULL) {
                                 pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_QUERY_INFO,
-                                                                 SMB2_STATUS_UNSUCCESSFUL,
-                                                                 NULL, cb_data);
+                                                                 SMB2_STATUS_UNSUCCESSFUL, NULL,
+                                                                 cb_data);
                         }
                 }
         }
@@ -4089,8 +3857,8 @@ smb2_query_info_request_cb(struct smb2_server *server, struct smb2_context *smb2
         smb2_server_log_err(SMB2_QUERY_INFO, ret, pdu);
 }
 
-static void
-smb2_set_info_request_cb(struct smb2_server *server, struct smb2_context *smb2, void *command_data, void *cb_data)
+static void smb2_set_info_request_cb(struct smb2_server *server, struct smb2_context *smb2,
+                                     void *command_data, void *cb_data)
 {
         struct smb2_set_info_request *req = command_data;
         struct smb2_error_reply err;
@@ -4104,15 +3872,14 @@ smb2_set_info_request_cb(struct smb2_server *server, struct smb2_context *smb2,
         }
         smb2_server_log_trace(SMB2_SET_INFO, req->file_info_class, ret);
         if (ret < 0) {
-                pdu = smb2_cmd_error_reply_async(smb2,
-                                &err, SMB2_SET_INFO, smb2_server_status_from_errno(SMB2_SET_INFO, ret), NULL, cb_data);
-        }
-        else if (!ret) {
+                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_SET_INFO,
+                                                 smb2_server_status_from_errno(SMB2_SET_INFO, ret),
+                                                 NULL, cb_data);
+        } else if (!ret) {
                 pdu = smb2_cmd_set_info_reply_async(smb2, req, NULL, cb_data);
                 if (pdu == NULL) {
                         pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_SET_INFO,
-                                                         SMB2_STATUS_UNSUCCESSFUL,
-                                                         NULL, cb_data);
+                                                         SMB2_STATUS_UNSUCCESSFUL, NULL, cb_data);
                 }
         }
         if (pdu != NULL) {
@@ -4122,13 +3889,12 @@ smb2_set_info_request_cb(struct smb2_server *server, struct smb2_context *smb2,
         smb2_server_log_err(SMB2_SET_INFO, ret, pdu);
 }
 
-static void
-smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *command_data, void *cb_data);
+static void smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *command_data,
+                                          void *cb_data);
 
 /* Replace the all-0xff compound placeholder file id with the file id
  * returned by the CREATE earlier in this compound (MS-SMB2 3.2.4.1). */
-static void
-smb2_server_fixup_compound_fid(struct smb2_context *smb2, int command, void *d)
+static void smb2_server_fixup_compound_fid(struct smb2_context *smb2, int command, void *d)
 {
         smb2_file_id *fid = NULL;
 
@@ -4171,8 +3937,8 @@ smb2_server_fixup_compound_fid(struct smb2_context *smb2, int command, void *d)
         }
 }
 
-static void
-smb2_general_client_request_cb(struct smb2_context *smb2, int status, void *command_data, void *cb_data)
+static void smb2_general_client_request_cb(struct smb2_context *smb2, int status,
+                                           void *command_data, void *cb_data)
 {
         struct connect_data *c_data = cb_data;
         struct smb2_server *server = c_data->server_context;
@@ -4180,8 +3946,7 @@ smb2_general_client_request_cb(struct smb2_context *smb2, int status, void *comm
         smb2_command_cb next_cb = smb2_general_client_request_cb;
 
         if (status == SMB2_STATUS_SUCCESS && smb2->pdu) {
-                smb2_server_fixup_compound_fid(smb2, smb2->pdu->header.command,
-                                               command_data);
+                smb2_server_fixup_compound_fid(smb2, smb2->pdu->header.command, command_data);
         }
 
         if (!smb2->pdu) {
@@ -4260,19 +4025,21 @@ smb2_general_client_request_cb(struct smb2_context *smb2, int status, void *comm
         }
 
         if (next_cb) {
-                /* alloc a pdu for next request. note that we dont really expect a tree connect, its just to
-                 * allow pdu reading to know to allow for any command above negotiate and session-setup
+                /* alloc a pdu for next request. note that we dont really expect a tree connect, its
+                 * just to allow pdu reading to know to allow for any command above negotiate and
+                 * session-setup
                  */
                 smb2->next_pdu = smb2_allocate_pdu(smb2, next_cmd, next_cb, cb_data);
                 if (!smb2->next_pdu) {
-                        smb2_set_error(smb2, "can not alloc pdu for authorization session setup request");
+                        smb2_set_error(smb2,
+                                       "can not alloc pdu for authorization session setup request");
                         smb2_close_context(smb2);
                 }
         }
 }
 
-static void
-smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *command_data, void *cb_data)
+static void smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *command_data,
+                                          void *cb_data)
 {
         struct connect_data *c_data = cb_data;
         struct smb2_server *server = c_data->server_context;
@@ -4309,20 +4076,17 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
                  * insist on a valid message
                  * TODO - perhaps use krb5+ntlmssp if configured?
                  */
-                if (ntlmssp_get_message_type(smb2,
-                                req->security_buffer, req->security_buffer_length,
-                                smb2->sec == SMB2_SEC_UNDEFINED,
-                                &message_type,
-                                &response_token, &response_length,
-                                &is_spnego_wrapped) >= 0) {
+                if (ntlmssp_get_message_type(
+                        smb2, req->security_buffer, req->security_buffer_length,
+                        smb2->sec == SMB2_SEC_UNDEFINED, &message_type, &response_token,
+                        &response_length, &is_spnego_wrapped) >= 0) {
                         smb2->sec = SMB2_SEC_NTLMSSP;
                 } else {
 
 #ifdef HAVE_LIBKRB5
                         smb2->sec = SMB2_SEC_KRB5;
 #else
-                        smb2_set_error(smb2, "No message type in NTLMSSP %s",
-                                        smb2_get_error(smb2));
+                        smb2_set_error(smb2, "No message type in NTLMSSP %s", smb2_get_error(smb2));
                         smb2_close_context(smb2);
                         return;
 #endif
@@ -4330,20 +4094,17 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
         }
 
         if (smb2->sec == SMB2_SEC_NTLMSSP) {
-                /* set error code in header - more processing required if negotiate req not auth req */
+                /* set error code in header - more processing required if negotiate req not auth req
+                 */
                 if (message_type == NEGOTIATE_MESSAGE) {
                         if (c_data->auth_data) {
                                 ntlmssp_destroy_context(c_data->auth_data);
                         }
-                        c_data->auth_data = ntlmssp_init_context(
-                                        "",
-                                        "",
-                                        "",
-                                        server->hostname,
-                                        smb2->client_challenge
-                                        );
+                        c_data->auth_data = ntlmssp_init_context("", "", "", server->hostname,
+                                                                 smb2->client_challenge);
                         if (!c_data->auth_data) {
-                                smb2_set_error(smb2, "can not init auth data %s", smb2_get_error(smb2));
+                                smb2_set_error(smb2, "can not init auth data %s",
+                                               smb2_get_error(smb2));
                                 smb2_close_context(smb2);
                                 return;
                         }
@@ -4351,36 +4112,34 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
 
                         /* alloc a pdu for next request */
                         smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_SESSION_SETUP,
-                                       smb2_session_setup_request_cb, cb_data);
+                                                           smb2_session_setup_request_cb, cb_data);
                         more_processing_needed = 1;
                         smb2->session_id = server->session_counter++;
-                }
-                else if (message_type == AUTHENTICATION_MESSAGE) {
+                } else if (message_type == AUTHENTICATION_MESSAGE) {
                         /* alloc a pdu for next request (not really required to get tree connect) */
                         smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_TREE_CONNECT,
-                                       smb2_general_client_request_cb, cb_data);
-                }
-                else {
+                                                           smb2_general_client_request_cb, cb_data);
+                } else {
                         smb2_set_error(smb2, "Unexpected ntlmssp msg code %08X", message_type);
                         smb2_close_context(smb2);
                         return;
                 }
-                if (ntlmssp_generate_blob(server, smb2, 0, c_data->auth_data,
-                                          req->security_buffer, req->security_buffer_length,
-                                          &rep.security_buffer,
+                if (ntlmssp_generate_blob(server, smb2, 0, c_data->auth_data, req->security_buffer,
+                                          req->security_buffer_length, &rep.security_buffer,
                                           &rep.security_buffer_length) < 0) {
                         smb2_close_context(smb2);
                         return;
                 }
                 if (message_type == AUTHENTICATION_MESSAGE) {
                         if (!ntlmssp_get_authenticated(c_data->auth_data)) {
-                                smb2_set_error(smb2, "Authentication failed: %s", smb2_get_error(smb2));
+                                smb2_set_error(smb2, "Authentication failed: %s",
+                                               smb2_get_error(smb2));
                                 if (server->handlers && server->handlers->auth_failed) {
                                         server->handlers->auth_failed(server, smb2);
                                 }
-                                pdu = smb2_cmd_error_reply_async(smb2,
-                                                &err, SMB2_SESSION_SETUP,
-                                                SMB2_STATUS_LOGON_FAILURE, NULL, cb_data);
+                                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_SESSION_SETUP,
+                                                                 SMB2_STATUS_LOGON_FAILURE, NULL,
+                                                                 cb_data);
                                 if (pdu != NULL) {
                                         smb2_set_pdu_message_id(smb2, pdu, smb2->message_id);
                                         smb2_queue_pdu(smb2, pdu);
@@ -4389,8 +4148,7 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
                                 smb2_close_context(smb2);
                                 return;
                         }
-                        if (ntlmssp_get_session_key(c_data->auth_data,
-                                                    &smb2->session_key,
+                        if (ntlmssp_get_session_key(c_data->auth_data, &smb2->session_key,
                                                     &smb2->session_key_size) < 0) {
                                 have_valid_session_key = 0;
                         }
@@ -4401,20 +4159,19 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
                 if (!c_data->auth_data) {
                         c_data->auth_data = krb5_init_server_client_cred(server, smb2, NULL);
                         if (!c_data->auth_data) {
-                                smb2_set_error(smb2, "can not init auth data %s", smb2_get_error(smb2));
+                                smb2_set_error(smb2, "can not init auth data %s",
+                                               smb2_get_error(smb2));
                                 smb2_close_context(smb2);
                                 return;
                         }
                         smb2->connect_data = c_data;
-                        if  (!smb2->session_id) {
+                        if (!smb2->session_id) {
                                 smb2->session_id = server->session_counter++;
                         }
                 }
 
-                if (krb5_session_reply(smb2, c_data->auth_data,
-                                         req->security_buffer,
-                                         req->security_buffer_length,
-                                         &more_processing_needed)) {
+                if (krb5_session_reply(smb2, c_data->auth_data, req->security_buffer,
+                                       req->security_buffer_length, &more_processing_needed)) {
                         smb2_close_context(smb2);
                         return;
                 }
@@ -4422,16 +4179,14 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
                 /* alloc a pdu for next request */
                 if (more_processing_needed) {
                         smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_SESSION_SETUP,
-                                                smb2_session_setup_request_cb, cb_data);
+                                                           smb2_session_setup_request_cb, cb_data);
                         smb2->session_id = server->session_counter++;
                 } else {
                         smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_TREE_CONNECT,
-                                                smb2_general_client_request_cb, cb_data);
+                                                           smb2_general_client_request_cb, cb_data);
                 }
-                rep.security_buffer_length =
-                        krb5_get_output_token_length(c_data->auth_data);
-                rep.security_buffer =
-                        krb5_get_output_token_buffer(c_data->auth_data);
+                rep.security_buffer_length = krb5_get_output_token_length(c_data->auth_data);
+                rep.security_buffer = krb5_get_output_token_buffer(c_data->auth_data);
 
                 if (!krb5_session_get_session_key(smb2, c_data->auth_data)) {
                         have_valid_session_key = 1;
@@ -4440,23 +4195,23 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
 #endif
         if (smb2->sign && have_valid_session_key == 0) {
                 smb2_close_context(smb2);
-                smb2_set_error(smb2, "Signing required by server. Session "
+                smb2_set_error(smb2,
+                               "Signing required by server. Session "
                                "Key is not available %s",
                                smb2_get_error(smb2));
                 smb2_close_context(smb2);
                 return;
         }
 
-        if (smb2->sign)  {
+        if (smb2->sign) {
                 /* Derive the signing key from session key
-                * This is based on negotiated protocol
-                */
+                 * This is based on negotiated protocol
+                 */
                 smb2_create_signing_key(smb2);
         }
 
-        if (server->allow_anonymous &&
-                         ((smb2->user == NULL || smb2->user[0] == '\0')||
-                         (smb2->password == NULL || smb2->password[0] == '\0'))) {
+        if (server->allow_anonymous && ((smb2->user == NULL || smb2->user[0] == '\0') ||
+                                        (smb2->password == NULL || smb2->password[0] == '\0'))) {
                 rep.session_flags |= SMB2_SESSION_FLAG_IS_GUEST;
         }
 
@@ -4475,8 +4230,7 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
                 }
                 if (more_processing_needed) {
                         pdu->header.status = SMB2_STATUS_MORE_PROCESSING_REQUIRED;
-                }
-                else {
+                } else {
                         /* Final session-setup reply is about to go out: every
                          * later PDU on a sealed connection must be a transform. */
                         smb2->session_up = 1;
@@ -4487,11 +4241,10 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
                                         smb2_close_context(smb2);
                                         return;
                                 }
-                        }
-                        else {
-                                pdu = smb2_cmd_error_reply_async(smb2,
-                                                &err, SMB2_SESSION_SETUP,
-                                                SMB2_STATUS_NOT_IMPLEMENTED, NULL, cb_data);
+                        } else {
+                                pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_SESSION_SETUP,
+                                                                 SMB2_STATUS_NOT_IMPLEMENTED, NULL,
+                                                                 cb_data);
                         }
                 }
         }
@@ -4512,8 +4265,8 @@ smb2_session_setup_request_cb(struct smb2_context *smb2, int status, void *comma
         }
 }
 
-static void
-smb2_negotiate_request_cb(struct smb2_context *smb2, int status, void *command_data, void *cb_data)
+static void smb2_negotiate_request_cb(struct smb2_context *smb2, int status, void *command_data,
+                                      void *cb_data)
 {
         struct connect_data *c_data = cb_data;
         struct smb2_server *server = c_data->server_context;
@@ -4587,13 +4340,16 @@ dialects_ready:
                 if (req->dialect_count == 0) {
                         /* windows does this crap */
                         /* alloc a pdu for another negotiate  request */
-                        smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_NEGOTIATE, smb2_negotiate_request_cb, cb_data);
+                        smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_NEGOTIATE,
+                                                           smb2_negotiate_request_cb, cb_data);
                         if (!smb2->next_pdu) {
-                                smb2_set_error(smb2, "can not alloc pdu for second negotiate request");
+                                smb2_set_error(smb2,
+                                               "can not alloc pdu for second negotiate request");
                                 smb2_close_context(smb2);
                         }
-                        pdu = smb2_cmd_error_reply_async(smb2,
-                                        &err, SMB2_NEGOTIATE, SMB2_STATUS_INVALID_PARAMETER, NULL, cb_data);
+                        pdu = smb2_cmd_error_reply_async(smb2, &err, SMB2_NEGOTIATE,
+                                                         SMB2_STATUS_INVALID_PARAMETER, NULL,
+                                                         cb_data);
                         if (pdu == NULL) {
                                 return;
                         }
@@ -4602,8 +4358,7 @@ dialects_ready:
                         return;
                 }
                 smb2->dialect = 0;
-                for (dialect_index = req->dialect_count - 1;
-                               dialect_index >= 0; dialect_index--) {
+                for (dialect_index = req->dialect_count - 1; dialect_index >= 0; dialect_index--) {
                         for (d = dialect_count - 1; d >= 0; d--) {
                                 if (dialects[d] == req->dialects[dialect_index]) {
                                         smb2->dialect = dialects[d];
@@ -4622,8 +4377,7 @@ dialects_ready:
                 }
 
                 smb2_set_client_guid(smb2, req->client_guid);
-        }
-        else {
+        } else {
                 /* an smb1-negotiate, list all dialects */
                 smb2->dialect = SMB2_VERSION_WILDCARD;
         }
@@ -4633,10 +4387,8 @@ dialects_ready:
 
         if (req) {
                 rep.capabilities = SMB2_GLOBAL_CAP_LARGE_MTU;
-                if (smb2->version == SMB2_VERSION_ANY  ||
-                    smb2->version == SMB2_VERSION_ANY3 ||
-                    smb2->version == SMB2_VERSION_0300 ||
-                    smb2->version == SMB2_VERSION_0302 ||
+                if (smb2->version == SMB2_VERSION_ANY || smb2->version == SMB2_VERSION_ANY3 ||
+                    smb2->version == SMB2_VERSION_0300 || smb2->version == SMB2_VERSION_0302 ||
                     smb2->version == SMB2_VERSION_0311) {
                         rep.capabilities |= SMB2_GLOBAL_CAP_ENCRYPTION;
                 }
@@ -4648,11 +4400,11 @@ dialects_ready:
                         }
                 }
 
-                if (smb2->seal && (smb2->dialect == SMB2_VERSION_0300 ||
-                                   smb2->dialect == SMB2_VERSION_0302)) {
-                        if(!(req->capabilities & SMB2_GLOBAL_CAP_ENCRYPTION)) {
+                if (smb2->seal &&
+                    (smb2->dialect == SMB2_VERSION_0300 || smb2->dialect == SMB2_VERSION_0302)) {
+                        if (!(req->capabilities & SMB2_GLOBAL_CAP_ENCRYPTION)) {
                                 smb2_set_error(smb2, "Encryption requested but client "
-                                               "does not support encryption.");
+                                                     "does not support encryption.");
                                 smb2_close_context(smb2);
                                 return;
                         }
@@ -4662,8 +4414,7 @@ dialects_ready:
                         will_sign = 1;
                 }
 
-                if (!server->allow_anonymous ||
-                                (smb2->password && smb2->password[0])) {
+                if (!server->allow_anonymous || (smb2->password && smb2->password[0])) {
                         if (smb2->dialect == SMB2_VERSION_0210) {
                                 /* smb2.1 requires signing if enabled on both sides
                                  * regardless of what the flags say */
@@ -4693,21 +4444,22 @@ dialects_ready:
                                 smb2->sign = 1;
                         } else {
                                 smb2_set_error(smb2, "Signing required but server "
-                                               "does not have signing enabled.");
+                                                     "does not have signing enabled.");
                                 smb2_close_context(smb2);
                                 return;
                         }
                 }
         }
 
-        rep.security_mode = (server->signing_enabled ? SMB2_NEGOTIATE_SIGNING_ENABLED : 0)|
-                             (smb2->sign ? SMB2_NEGOTIATE_SIGNING_REQUIRED : 0);
+        rep.security_mode = (server->signing_enabled ? SMB2_NEGOTIATE_SIGNING_ENABLED : 0) |
+                            (smb2->sign ? SMB2_NEGOTIATE_SIGNING_REQUIRED : 0);
         memcpy(rep.server_guid, server->guid, 16); /* TODO */
-        rep.max_transact_size  = smb2->max_transact_size;;
-        rep.max_read_size      = smb2->max_read_size;
-        rep.max_write_size     = smb2->max_write_size;
-        rep.dialect_revision   = smb2->dialect;
-        rep.cypher             = smb2->cypher;
+        rep.max_transact_size = smb2->max_transact_size;
+        ;
+        rep.max_read_size = smb2->max_read_size;
+        rep.max_write_size = smb2->max_write_size;
+        rep.dialect_revision = smb2->dialect;
+        rep.cypher = smb2->cypher;
 
         /* remember negotiated capabilites and security mode */
         smb2->capabilities = rep.capabilities;
@@ -4721,9 +4473,8 @@ dialects_ready:
         rep.server_start_time = smb2_timeval_to_win(&now);
 
         rep.security_buffer_length = smb2_spnego_create_negotiate_reply_blob(
-                                        smb2,
-                                        (smb2->sec == SMB2_SEC_UNDEFINED || smb2->sec == SMB2_SEC_NTLMSSP),
-                                        (void*)&rep.security_buffer);
+            smb2, (smb2->sec == SMB2_SEC_UNDEFINED || smb2->sec == SMB2_SEC_NTLMSSP),
+            (void *)&rep.security_buffer);
 
         pdu = smb2_cmd_negotiate_reply_async(smb2, &rep, NULL, cb_data);
         if (rep.security_buffer) {
@@ -4738,15 +4489,16 @@ dialects_ready:
 
         if (req) {
                 /* alloc a pdu for session request */
-                smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_SESSION_SETUP, smb2_session_setup_request_cb, cb_data);
+                smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_SESSION_SETUP,
+                                                   smb2_session_setup_request_cb, cb_data);
                 if (!smb2->next_pdu) {
                         smb2_set_error(smb2, "can not alloc pdu for session setup request");
                         smb2_close_context(smb2);
                 }
-        }
-        else {
+        } else {
                 /* alloc a pdu for another negotiate  request */
-                smb2->next_pdu = smb2_allocate_pdu(smb2, SMB2_NEGOTIATE, smb2_negotiate_request_cb, cb_data);
+                smb2->next_pdu =
+                    smb2_allocate_pdu(smb2, SMB2_NEGOTIATE, smb2_negotiate_request_cb, cb_data);
                 if (!smb2->next_pdu) {
                         smb2_set_error(smb2, "can not alloc pdu for second negotiate request");
                         smb2_close_context(smb2);
@@ -4754,11 +4506,10 @@ dialects_ready:
         }
 }
 
-static int
-accept_cb(const int fd, void *cb_data)
+static int accept_cb(const int fd, void *cb_data)
 {
         int err = -1;
-        struct smb2_context **psmb2 = (struct smb2_context**)cb_data;
+        struct smb2_context **psmb2 = (struct smb2_context **)cb_data;
         struct smb2_context *smb2;
 
         if (!psmb2) {
@@ -4770,8 +4521,7 @@ accept_cb(const int fd, void *cb_data)
         smb2 = smb2_init_context();
         if (smb2 == NULL) {
                 err = -ENOMEM;
-        }
-        else {
+        } else {
                 *psmb2 = smb2;
                 /* put client fd into connecting fd array (todo? for now just set fd) */
                 smb2->fd = fd;
@@ -4789,7 +4539,8 @@ int smb2_serve_port_async(const int fd, const int to_msecs, struct smb2_context
         return err;
 }
 
-int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_client_connection cb, void *cb_data)
+int smb2_serve_port(struct smb2_server *server, const int max_connections,
+                    smb2_client_connection cb, void *cb_data)
 {
         struct smb2_context *smb2;
         struct connect_data *c_data = cb_data;
@@ -4818,7 +4569,7 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
         }
         if (!server->domain[0]) {
                 strncpy(server->domain, default_domain,
-                               MIN(sizeof(server->domain),strlen(default_domain) + 1));
+                        MIN(sizeof(server->domain), strlen(default_domain) + 1));
         }
 
 #ifdef HAVE_LIBKRB5
@@ -4827,8 +4578,8 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                 return err;
         }
 #endif
-        err = smb2_bind_and_listen_ip(server->bind_ipv4, server->port,
-                                      max_connections, &server->fd);
+        err =
+            smb2_bind_and_listen_ip(server->bind_ipv4, server->port, max_connections, &server->fd);
         if (err != 0) {
                 return err;
         }
@@ -4838,8 +4589,8 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                 if (server->fd < 0) {
                         break;
                 }
-                /* select on the file descriptors of all active client connections and our server socket
-                   for the first readable event
+                /* select on the file descriptors of all active client connections and our server
+                   socket for the first readable event
                 */
                 FD_ZERO(&rfds);
                 FD_ZERO(&wfds);
@@ -4874,13 +4625,8 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                 timeout.tv_sec = 0;
                 timeout.tv_usec = 100000;
 
-                ready = select(
-                            maxfd + 1,
-                            &rfds,
-                            &wfds,
-                            NULL,
-                            (timeout.tv_sec > 0 || timeout.tv_usec >= 0) ? &timeout : NULL
-                           );
+                ready = select(maxfd + 1, &rfds, &wfds, NULL,
+                               (timeout.tv_sec > 0 || timeout.tv_usec >= 0) ? &timeout : NULL);
 
                 if (ready > 0) {
                         now = time(NULL);
@@ -4890,18 +4636,24 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                                 if (smb2->owning_server != server) {
                                         continue;
                                 }
-                                if (SMB2_VALID_SOCKET(smb2_get_fd(smb2)) && FD_ISSET(smb2_get_fd(smb2), &rfds)) {
+                                if (SMB2_VALID_SOCKET(smb2_get_fd(smb2)) &&
+                                    FD_ISSET(smb2_get_fd(smb2), &rfds)) {
                                         if (smb2_service(smb2, POLLIN) < 0) {
-                                                smb2_set_error(smb2, "smb2_service (in) failed with : "
-                                                                "%s", smb2_get_error(smb2));
+                                                smb2_set_error(smb2,
+                                                               "smb2_service (in) failed with : "
+                                                               "%s",
+                                                               smb2_get_error(smb2));
                                                 smb2_close_context(smb2);
                                         }
                                         err = 0;
                                 }
-                                if (SMB2_VALID_SOCKET(smb2_get_fd(smb2)) && FD_ISSET(smb2_get_fd(smb2), &wfds)) {
+                                if (SMB2_VALID_SOCKET(smb2_get_fd(smb2)) &&
+                                    FD_ISSET(smb2_get_fd(smb2), &wfds)) {
                                         if (smb2_service(smb2, POLLOUT) < 0) {
-                                                smb2_set_error(smb2, "smb2_service (out) failed with : "
-                                                                "%s", smb2_get_error(smb2));
+                                                smb2_set_error(smb2,
+                                                               "smb2_service (out) failed with : "
+                                                               "%s",
+                                                               smb2_get_error(smb2));
                                                 smb2_close_context(smb2);
                                         }
                                 }
@@ -4916,14 +4668,18 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                                  * wfds at select(); flush them now. */
                                 if (SMB2_VALID_SOCKET(smb2_get_fd(smb2)) && smb2->outqueue) {
                                         if (smb2_service(smb2, POLLOUT) < 0) {
-                                                smb2_set_error(smb2, "smb2_service (out-flush) failed with : "
-                                                                "%s", smb2_get_error(smb2));
+                                                smb2_set_error(
+                                                    smb2,
+                                                    "smb2_service (out-flush) failed with : "
+                                                    "%s",
+                                                    smb2_get_error(smb2));
                                                 smb2_close_context(smb2);
                                         }
                                 }
-                                if (!SMB2_VALID_SOCKET(smb2->fd) && ((time(NULL) - now) > (smb2->timeout)))
-                                {
-                                        smb2_set_error(smb2, "Timeout expired and no connection exists");
+                                if (!SMB2_VALID_SOCKET(smb2->fd) &&
+                                    ((time(NULL) - now) > (smb2->timeout))) {
+                                        smb2_set_error(smb2,
+                                                       "Timeout expired and no connection exists");
                                         smb2_close_context(smb2);
                                 }
                                 if (smb2->timeout) {
@@ -4937,29 +4693,33 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                                 if (!err && smb2) {
                                         c_data = calloc(1, sizeof(struct connect_data));
                                         if (c_data == NULL) {
-                                                smb2_set_error(smb2, "Failed to allocate connect_data");
+                                                smb2_set_error(smb2,
+                                                               "Failed to allocate connect_data");
                                                 smb2_close_context(smb2);
                                         }
                                         c_data->server_context = server;
                                         smb2->connect_data = c_data;
 
                                         /* alloc a pdu for first server request */
-                                        smb2->pdu = smb2_allocate_pdu(smb2, SMB2_NEGOTIATE, smb2_negotiate_request_cb, c_data);
+                                        smb2->pdu =
+                                            smb2_allocate_pdu(smb2, SMB2_NEGOTIATE,
+                                                              smb2_negotiate_request_cb, c_data);
                                         if (!smb2->pdu) {
-                                                smb2_set_error(smb2, "can not alloc pdu for request");
+                                                smb2_set_error(smb2,
+                                                               "can not alloc pdu for request");
                                                 smb2_close_context(smb2);
                                         }
-                                        /* got a new smb2 context with a connection, enlist it and tell user */
+                                        /* got a new smb2 context with a connection, enlist it and
+                                         * tell user */
                                         smb2->owning_server = server;
                                         smb2->max_transact_size = server->max_transact_size;
-                                        smb2->max_read_size     = server->max_read_size;
-                                        smb2->max_write_size    = server->max_write_size;
+                                        smb2->max_read_size = server->max_read_size;
+                                        smb2->max_write_size = server->max_write_size;
 
                                         if (cb) {
                                                 cb(smb2, cb_data);
                                         }
-                                }
-                                else if (err) {
+                                } else if (err) {
                                         break;
                                 }
                         }
@@ -4970,8 +4730,10 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                         for (smb2 = smb2_active_contexts(); smb2; smb2 = smb2->next) {
                                 if (smb2->owning_server == server) {
                                         if (!SMB2_VALID_SOCKET(smb2_get_fd(smb2))) {
-                                                if (server->handlers && server->handlers->destruction_event) {
-                                                        server->handlers->destruction_event(server, smb2);
+                                                if (server->handlers &&
+                                                    server->handlers->destruction_event) {
+                                                        server->handlers->destruction_event(server,
+                                                                                            smb2);
                                                 }
                                                 smb2_destroy_context(smb2);
                                                 break;
@@ -5006,7 +4768,7 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                 if (credential_renewal_time < now) {
                         int rerr;
 
-                        credential_renewal_time = now + 60*60*24;
+                        credential_renewal_time = now + 60 * 60 * 24;
                         rerr = krb5_renew_server_credentials(server);
                         if (rerr) {
                                 /* Keep serving; NTLM/guest still work. */
@@ -5019,8 +4781,7 @@ int smb2_serve_port(struct smb2_server *server, const int max_connections, smb2_
                         }
                 }
 #endif
-        }
-        while (err == 0);
+        } while (err == 0);
 
         close(server->fd);
         server->fd = -1;
